---
title: "2026-06-17_dotey_宝玉_之所以研究_Claude_Design_是因为最近摸索出一套不错的开发模式_1"
source: "https://x.com/dotey/status/2063674134903603302"
author:
  - "[[@dotey]]"
published: 2026-06-17
created: 2026-06-17
description:
tags:
  - "x"
  - "@dotey"
  - "claude"
  - "design"
---

# 宝玉: 之所以研究 Claude Design，是因为最近摸索出一套不错的开发模式： 1

**宝玉**

之所以研究 Claude Design，是因为最近摸索出一套不错的开发模式：

1\. 先用 Claude Design 去设计 App 的 UI 和 UX，第一版本越简单越好。

Claude Design 交付的结果是 HTML + CSS + React + data.js，通过 CSS 一下就可以看清楚设计系统的颜色系统、尺寸规范，通过 React 可以看清楚组件结构，通过 data 可以知道数据结构什么样子。

这几个组合可比 figma 对于 AI 来说友好多了，尤其是 data.jsx，这是一般的设计 App 不会有的，但对开发特别重要的。

还有一个优势就是交付的结果都是文本格式的，可以一起提交到 git 做版本管理，用 git diff 就可以清楚的让 AI 知道设计稿修改了什么。

2\. 把 Claude Design 生成设计稿交给 Claude Opus 4.8 去实现一个 MVP，第一个版本只实现 UI 功能，告诉它技术栈（比如 AppKit 而不是 SwiftUI），通过几个小版本的迭代，让它逐步完善。比如第一个版本只是主界面

之所以不用 GPT，是因为 Opus 在 UI 实现上比 GPT 好太多，同样的设计稿，GPT 实现的各种细节问题的，不能很好的遵守设计稿。

之所以不一下子实现太多功能，是因为功能越少 Agent 越容易聚焦，做出来效果越好。所以怎么拆分版本，也是用好 Coding Agent 的一种能力。

非 UI 的功能，用什么模型没有那么大差别，所以我也会用 Codex 配合非 UI 工作。

3\. 每次生成一个版本，要让它可以运行，无论是 Agent 还是人都是需要即时反馈的，有了反馈才知道方向对不对，才知道下一步要做什么。

人在测试的时候，能发现各种问题，代码问题就让 Agent 去修改，UI 设计有问题不是马上修改代码，而是要去 Claude Design 去修改设计稿，设计稿修改完了，把设计稿下载回去，替换本地的设计稿文件。

设计稿替换后，让 Agent 去 git diff 分析一下变更，给它个截图，都不用多说它就知道怎么改了。

4\. 版本稳定后继续新一轮迭代

当设计的功能做完之后，不一定是要马上去设计新的功能，而是可以重新审查一下实现的代码，重构优化一下。

比如我在第一版实现后，让 Opus 4.8 去做了性能分析，看性能问题在哪，然后告诉我当前用 NSScrollView，内容一多就内存暴涨，建议 NSTableView。

我心想这不应该是一开始就该考虑到的么！不管怎么样亡羊补牢也不算晚。

性能优化完就继续加功能，更新设计稿，让 Agent 参考新设计稿优化功能。

\---

再回来说研究 Claude Design 的事，因为反复 Claude Design 修改，同步到本地，然后这一步让我后来很烦，因为需要在本地和 Claude Design 网页切换，还要导出下载到本地，解压缩替换。

于是我就想能不能在本地就可以重现 Claude Design 直接集成到本地 Agent，所以我去研究了 Claude Design，然后发现完全可以本地跑起来，只是本地 Agent 在工具调用上有些差异，以及不方便直接在设计稿上标记编辑。

上周正好 Codex 发布了直接调试 iOS 的功能，它带了标记修改的功能，然后我灵机一动，这不正好可以代替 Claude Design 的标记修改功能么。（图2）

问题是 GPT 5.5 模型设计能力不够，在 Codex 里面设计效果也不会好。

接着 Cursor 也发布了网页标记功能，这下正好，Cursor 里面可以用 Opus 4.8，做成 Skill 就可以本地运行了，还可以在 Cursor 内置浏览器中，直接标记、评论修改。

好在 Claude Code 的核心逻辑都在浏览器前端，他们在网页上做了个 Harness，这给了我分析的便利，耐心一点就可以分析出所有的 Prompt、内置 Skills、初始组件，再针对 Cursor 的工具做一些兼容就可以跑起来了。（图3）

测试对比了一下和在 Claude Design 上跑的效果差不多。（图4）

归根结底，还是 Claude Desktop 太拉胯了，Claude Design 本应该集成在 Claude Desktop 的，不然也不需要我这么折腾。

当然不折腾下我也没机会学习 Claude Design 优秀的设计，真的很棒，接下来会系列更新我的学习心得。

推荐去试试最终的 Skill，让你本地就可以跑 Claude Design：

[https://github.com/jimliu/baoyu-design…](https://github.com/jimliu/baoyu-design)

![图片](https://pbs.twimg.com/media/HKOklkXWcAArKsq?format=jpg&name=large)![图片](https://pbs.twimg.com/media/HKOkmlLXYAAS7aU?format=jpg&name=large)![图片](https://pbs.twimg.com/media/HKOko4SWwAAOHMg?format=jpg&name=large)![图片](https://pbs.twimg.com/media/HKOkrzNXcAA032T?format=jpg&name=large)![图片](https://pbs.twimg.com/media/HKLu4EOXAAAzoZu?format=jpg&name=large)![图片](https://pbs.twimg.com/media/HKLvOXQWYAAy7SV?format=jpg&name=large)![图片](https://pbs.twimg.com/media/HKLwQ3ZWIAAdcsj?format=jpg&name=large)

> **@dotey**
> 
> 最近为了研究 Claude Design，专门写了一个工具，可以解析 HAR 文件，解密 Claude Design 传输的二进制内容，这样可以方便的看到请求的 Prompt x.com/dotey/status/2…

![引用图片](https://pbs.twimg.com/media/HKLu4EOXAAAzoZu?format=jpg&name=large)![引用图片](https://pbs.twimg.com/media/HKLvOXQWYAAy7SV?format=jpg&name=large)![引用图片](https://pbs.twimg.com/media/HKLwQ3ZWIAAdcsj?format=jpg&name=large)

* * *

### 热门回复

**@FriedRice Entertainment** ♥ 4.2K · 💬 16

NEX2Y, ready to vibe with NEXZ this July in Singapore?

Don’t wait long long until NEXZ time!

**@宝玉** ♥ 4 · 💬 0

Claude Design 有一个很重要的前提：你有自己的 DS（Design System）。当你有了 Design System，那么你不需要太多的 UI 设计，因为 UI 设计规范在 DS 已经定义好了，更多的是 UX 的设计。这才是正确的方向。

**@宝玉** ♥ 2 · 💬 1

如果配合好设计系统（Design System）去用 Claude Design，那么你就可以更多聚焦在 UX 而不是 UI 设计

**@saurabh** ♥ 1 · 💬 1

可靠的工作流程 但设计 UI 和设计 UX 不是同一个步骤，而这会将它们混为一谈。Claude 能快速输出简洁的界面。它无法告诉你这个界面是否应该存在，或者用户当时想做什么 在生成流程之前，你如何验证这个流程？

**@理工爸 | AI & 投资迭代** ♥ 0 · 💬 1

宝玉老师，你说的5.5设计能力不够仅是指ui这块吗？

---

<br>

## 💬 对话记录

### 📋 提示词模板: 解读

[Prompt: 解读]

### 🤖 AI

# 深度总结

这篇内容讲的是一套 **AI 辅助软件开发的新工作流**：  
先让 AI 做设计，再让 AI 写代码，中间通过可运行版本和 Git diff 持续迭代。

核心不是“让 AI 一次性做完整产品”，而是：

> **把设计、实现、反馈、迭代拆成小步，让 AI 在明确上下文中高质量工作。**

---

## 1. 核心开发流程

### 第一步：用 Claude Design 做 UI / UX 初稿

Claude Design 输出的不是传统设计稿，而是：

- `HTML`
- `CSS`
- `React`
- `data.js`

这比 Figma 更适合 AI 和开发，因为：

- CSS 能直接体现颜色、尺寸、设计规范
- React 能体现组件结构
- data.js 能体现数据结构
- 全部是文本，可进 Git
- Git diff 能清楚显示设计变更

因此，Claude Design 的价值不只是“画界面”，而是生成一套 **可被 AI 理解、可被代码实现、可被版本管理的设计资产**。

---

### 第二步：用 Claude Opus 实现 MVP

作者会把设计稿交给 Claude Opus 去实现第一个版本。

但重点是：

> **第一版只做 UI，不做复杂功能。**

原因是：

- 功能越少，Agent 越聚焦
- 越容易符合设计稿
- 越容易跑起来
- 越容易发现问题

作者认为，Opus 在 UI 实现上明显优于 GPT；而非 UI 功能，则模型差距没那么大，可以用 Codex 等工具配合完成。

---

### 第三步：每个版本都必须可运行

作者强调：

> **无论人还是 Agent，都需要即时反馈。**

每次生成版本，都要能运行。  
运行之后，人测试发现问题：

- 如果是代码问题：让 Agent 改代码
- 如果是设计问题：回 Claude Design 改设计稿
- 改完设计稿后同步到本地
- 让 Agent 看 Git diff 和截图，再更新实现

这里的关键是：

> 不要直接在代码里修设计问题，而是先修设计源头。

这样可以保证设计稿和代码长期一致。

---

### 第四步：稳定后再继续迭代

功能完成后，不急着加新功能，而是先做：

- 代码审查
- 性能分析
- 架构优化
- 重构

例如作者让 Opus 分析性能，发现 `NSScrollView` 在内容多时内存暴涨，建议换成 `NSTableView`。

这说明 AI 不只是写代码，也可以参与工程质量优化。

---

## 2. 作者为什么研究 Claude Design？

因为原流程有一个痛点：

> Claude Design 在网页端，设计稿需要反复导出、下载、解压、替换，本地开发体验很差。

于是作者开始研究 Claude Design 的内部机制，试图把它本地化，集成进本地 Agent 工作流。

后来他发现：

- Claude Design 的核心逻辑很多在浏览器前端
- 可以分析出 Prompt、Skills、初始组件等
- 再适配 Cursor 的工具能力
- 就能在本地复现类似 Claude Design 的效果

最终形成了一个本地 Skill：  
可以在 Cursor 中运行类似 Claude Design 的能力，并支持网页标记、评论、修改。

---

# 深入浅出解读

这套方法可以理解为：

> **把 AI 当成一个“设计 + 前端 + 工程助理团队”，但你要给它清晰流程和小任务。**

传统流程是：

```text
产品想法 → Figma 设计 → 开发实现 → 测试反馈 → 修改
```

作者的新流程是：

```text
想法
↓
Claude Design 生成可代码化设计稿
↓
Claude Opus 按设计稿实现 MVP
↓
运行测试
↓
发现问题
↓
改设计稿或改代码
↓
Git diff 告诉 AI 变化
↓
继续迭代
```

它的厉害之处在于：

## 1. 设计稿变成了“代码友好型资产”

Figma 对人友好，但对 AI 和程序不一定友好。  
Claude Design 输出的是文本代码，AI 更容易理解。

这意味着设计不再只是“图片”，而是：

- 结构清楚
- 样式清楚
- 数据清楚
- 可追踪变化
- 可直接转实现

---

## 2. 小步快跑，比一次性生成更可靠

作者反复强调：

> 不要让 Agent 一口气做太多。

AI 写代码最大的问题不是不会写，而是容易在复杂任务里失焦。

所以更好的方式是：

```text
先主界面
再交互
再数据
再性能
再功能扩展
```

这其实和传统工程里的敏捷开发很像，只是执行者变成了 AI Agent。

---

## 3. Git diff 成了 AI 的“变更说明书”

以前设计稿改了，开发要问：

- 哪里改了？
- 为什么改？
- 改了多少？
- 是视觉改动还是结构改动？

现在设计稿是文本，直接用 Git diff 就能看到变化。  
AI 看 diff 后，就知道该改代码哪里。

这大幅降低了沟通成本。

---

## 4. Design System 是前提

作者在回复里补充了一个重要观点：

> Claude Design 真正好用的前提，是你有自己的 Design System。

也就是说，AI 不应该每次重新发明 UI 风格。  
如果颜色、字号、间距、组件规范已经在设计系统里定义好，AI 就能少纠结 UI，多关注 UX。

更好的方向是：

```text
Design System 定义 UI 规范
Claude Design 生成 UX 和界面结构
Agent 负责实现和迭代
```

---

# 辩证思考

## 这套方法的价值

### 1. 它把 AI 开发从“生成代码”推进到“生成流程”

很多人用 AI 写代码，只是让它补函数、改 bug。  
作者这套方法更进一步：让 AI 参与完整产品迭代。

它不是单点工具，而是一条链路：

```text
设计 → 实现 → 运行 → 反馈 → 修改 → 重构
```

这才是 AI Coding 真正有价值的地方。

---

### 2. 它降低了个人开发者做产品的门槛

过去一个人做 App，要懂：

- 产品
- 交互
- UI
- 前端
- 后端
- 性能
- 测试

现在 AI 可以承担很多执行工作。  
人更像是导演：

- 定方向
- 拆任务
- 看结果
- 做判断
- 控制质量

这会显著提高独立开发者、小团队的生产力。

---

### 3. 它强调“反馈闭环”，这是关键

AI 开发最怕的是：

> 任务很大、反馈很晚、错误积累。

作者的方法要求每个版本都能运行，就是为了快速暴露问题。  
这比盲目追求“一次生成完整项目”更靠谱。

---

## 这套方法的局限

### 1. AI 仍然不能替代产品判断

有评论指出得很准确：

> Claude 能生成界面，但不一定知道这个界面是否应该存在。

AI 可以帮你做 UI，但不一定能判断：

- 用户真正要解决什么问题
- 这个流程是否必要
- 这个功能有没有价值
- 用户为什么要用它

所以，产品判断仍然需要人负责。

---

### 2. 对使用者要求并不低

这套流程看似自动化，但其实需要使用者具备较强能力：

- 会拆分版本
- 懂设计系统
- 能判断 UI 好坏
- 能读懂代码结构
- 能测试和反馈
- 能管理 Git diff
- 知道什么时候该重构

也就是说，AI 降低了执行成本，但没有消除专业判断。

---

### 3. 工具链仍然碎片化

作者之所以折腾本地 Claude Design，是因为现有工具割裂：

- Claude Design 在网页
- Cursor 在本地
- Claude Desktop 体验不足
- Codex、Opus、GPT 各有长短

未来真正理想的状态应该是：

```text
设计、编码、调试、标注、版本管理、运行反馈
全部在一个统一环境里完成
```

目前还没有完全成熟。

---

# 最终结论

这篇内容真正值得学习的不是某个工具，而是一种方法：

> **用 AI 做产品开发，不是让它一次性完成全部，而是建立一套可设计、可实现、可运行、可反馈、可迭代的闭环。**

最关键的三点是：

1. **设计稿要代码化**：让 AI 看得懂、改得动、可 diff  
2. **任务要小步化**：让 Agent 聚焦，降低失控概率  
3. **反馈要即时化**：每一版都能运行，问题及时暴露  

一句话概括：

> 未来高效开发者的核心能力，不是单纯会写代码，而是会组织 AI、拆分任务、管理反馈，并在关键节点做正确判断。

---

### 📋 提示词模板: 自动分类评分

[自动分类评分]

### 🤖 AI

## 自动分类评分结果

**标题**: Claude Design本地开发流

**评级**: A (74分)  |  **深度阅读**: 是  |  **建议操作**: 精读

**主类别**: AI开发工作流
**次类别**: AI辅助编程, 产品原型设计, 本地工具集成
**标签**: #Claude Design #Coding Agent #MVP #设计系统 #Cursor #AI开发

### 评分明细
| 维度 | 分数 | 权重 |
|------|------|------|
| 信息密度 | 7 | 10% |
| 新颖性 | 8 | 10% |
| 相关性 | 8 | 20% |
| 可操作性 | 8 | 15% |
| 方法论价值 | 8 | 15% |
| 可迁移性 | 7 | 10% |
| 证据质量 | 5 | 10% |
| 长期杠杆 | 7 | 10% |

**一句话点评**: 一篇高价值的AI辅助产品原型与开发工作流实践笔记，虽证据偏经验化，但可操作性很强。
**核心高价值点**: 将设计稿转化为可版本管理的文本资产，并用Git diff驱动Agent理解设计变更，是非常实用的AI协作模式。

### 摘要
文章介绍了用Claude Design生成HTML/CSS/React/data设计稿，再交给Claude Opus实现MVP，并通过Git diff、截图反馈、性能复盘和本地化Skill集成进行持续迭代的AI开发流程。

### 知识要点
- Claude Design输出HTML、CSS、React和data.js，比传统Figma设计稿更适合AI理解和后续代码实现。
- AI开发MVP时应先限制功能范围，让Agent聚焦实现可运行的最小版本，再小步迭代。
- 设计问题应回到设计稿层修改，再通过Git diff和截图让Agent同步实现，而不是直接在代码里修补。
- UI实现适合使用Claude Opus等设计遵循能力更强的模型，非UI功能可用其他Coding Agent配合。
- 设计系统是Claude Design发挥价值的重要前提，可让开发者更多关注UX而非重复处理UI规范。

### 行动项
- 尝试使用Claude Design生成一个简单App的HTML/CSS/React/data.js设计稿，并提交到Git进行版本管理。
- 用Claude Opus或类似Coding Agent基于设计稿实现一个只含UI功能的MVP，控制首版范围。
- 建立“设计稿修改—Git diff—Agent实现—运行反馈”的小步迭代流程。
- 评估并试用文中提到的baoyu-design项目，判断是否适合集成到本地Cursor工作流。
- 为自己的项目整理基础Design System，以便后续AI生成设计时保持一致性。

### 存档判定
归档: 是 — 原因: 提供了较完整的AI辅助UI设计到MVP实现的迭代流程，并包含本地化Claude Design工具链的实践线索，适合作为AI开发工作流参考。
删除: 否
留存决策: 长期保留

---

### 📋 提示词模板: 结构化

[Prompt: 结构化]

### 🤖 AI

## 主旨概括

作者研究 Claude Design，是因为他摸索出了一套“AI 辅助设计 + AI 辅助开发”的高效开发流程：先用 Claude Design 生成可读、可版本管理的设计稿，再交给 Coding Agent 实现和迭代。后来为了减少网页与本地切换的麻烦，他进一步研究并复刻了 Claude Design 的本地运行方式，做成了可在 Cursor 中使用的 Skill。

---

## 1. 用 Claude Design 先做 App 的 UI / UX 设计

作者的开发流程第一步，是先用 **Claude Design 设计 App 的 UI 和 UX**，而且第一版要尽量简单。

Claude Design 输出的设计稿包含：

- `HTML`
- `CSS`
- `React`
- `data.js / data.jsx`

这些内容相比 Figma 更适合 AI 理解：

- 通过 **CSS** 可以看清颜色系统、尺寸规范等设计系统；
- 通过 **React** 可以理解组件结构；
- 通过 **data.js / data.jsx** 可以了解数据结构，这对开发非常重要；
- 所有内容都是文本格式，可以提交到 **Git** 做版本管理；
- 后续修改时，可以通过 **git diff** 让 AI 清楚知道设计稿哪里变了。

作者认为，这种“文本化设计稿”比传统设计工具对 AI 更友好。

---

## 2. 把设计稿交给 Claude Opus 4.8 实现 MVP

第二步，是把 Claude Design 生成的设计稿交给 **Claude Opus 4.8**，让它实现一个 MVP。

作者的做法是：

- 第一版只实现 UI 功能；
- 明确告诉模型技术栈，例如使用 **AppKit** 而不是 SwiftUI；
- 通过多个小版本逐步迭代；
- 一开始不要实现太多功能，让 Agent 更容易聚焦。

作者特别提到：

- **Opus 4.8 在 UI 实现上明显优于 GPT**；
- 同样的设计稿，GPT 容易出现很多细节问题，也不太能严格遵守设计稿；
- 非 UI 功能方面，不同模型差别没那么大，所以作者也会用 **Codex** 处理非 UI 工作。

他总结出一个经验：  
**拆分版本、控制每轮功能范围，也是用好 Coding Agent 的关键能力。**

---

## 3. 每个版本都要能运行，靠即时反馈推动迭代

作者强调，每次生成一个版本，都必须让它可以运行。

原因是：

- 无论是人还是 Agent，都需要即时反馈；
- 只有运行起来，才能判断方向是否正确；
- 人在测试时可以发现各种问题。

处理问题时，作者会区分两类情况：

### 代码问题

如果是代码问题，就直接让 Agent 修改。

### UI / 设计问题

如果是 UI 设计有问题，作者不会马上改代码，而是：

1. 回到 Claude Design 修改设计稿；
2. 下载新的设计稿；
3. 替换本地设计稿文件；
4. 让 Agent 通过 `git diff` 分析变更；
5. 再给 Agent 一张截图，让它根据变化修改实现。

这样 Agent 基本不需要太多额外解释，就能理解应该怎么改。

---

## 4. 版本稳定后，先重构优化，再继续加功能

当某一轮设计功能完成后，作者不会马上进入新功能开发，而是会重新审查代码，做重构和优化。

例如：

- 第一版实现后，作者让 **Opus 4.8** 做性能分析；
- Opus 发现当前使用 `NSScrollView`，当内容变多时会导致内存暴涨；
- 它建议改用 `NSTableView`。

作者调侃说，这种问题本来应该一开始就考虑到，但“亡羊补牢也不算晚”。

优化完成后，再进入下一轮：

- 增加新功能；
- 更新设计稿；
- 让 Agent 参考新设计稿继续优化实现。

---

## 5. 为什么开始深入研究 Claude Design

在反复使用 Claude Design 的过程中，作者发现一个很烦的问题：

- 需要在本地开发环境和 Claude Design 网页之间来回切换；
- 每次修改设计稿都要导出、下载、解压、替换本地文件。

于是他开始思考：  
**能不能把 Claude Design 的能力直接集成到本地 Agent 中？**

经过研究后，他发现 Claude Design 完全可以在本地跑起来，只是存在两个问题：

- 本地 Agent 的工具调用方式和 Claude Design 有差异；
- 不方便像网页里那样直接在设计稿上标记和编辑。

---

## 6. Codex 和 Cursor 的新功能带来了解法

后来 Codex 发布了直接调试 iOS 的功能，并带有“标记修改”的能力。作者认为，这可以部分替代 Claude Design 的标记编辑能力。

但问题是：

- GPT 5.5 的设计能力不够强；
- 在 Codex 中做设计，效果并不好。

之后 Cursor 也发布了网页标记功能，这刚好满足作者需求：

- Cursor 里可以使用 **Opus 4.8**；
- 可以做成 Skill 在本地运行；
- 可以在 Cursor 内置浏览器中直接标记、评论、修改。

这让作者找到了把 Claude Design 本地化的可行方案。

---

## 7. 作者如何复刻 Claude Design 的本地能力

作者发现，Claude Design 的核心逻辑很多都在浏览器前端中。

Claude Design 在网页上做了一个 **Harness**，这方便了作者分析其运行机制。通过耐心分析，他提取出了：

- 所有 Prompt；
- 内置 Skills；
- 初始组件；
- 工具调用逻辑。

之后，他针对 Cursor 的工具系统做兼容，就让这套能力在本地跑了起来。

作者测试后认为，本地运行效果和 Claude Design 网页版差不多。

---

## 8. 作者的结论与推荐

作者认为，根本原因还是 **Claude Desktop 太弱**：

> Claude Design 本来就应该集成在 Claude Desktop 里，否则也不需要自己这么折腾。

不过，通过这次折腾，他也深入学习了 Claude Design 的优秀设计，并认为它“真的很棒”。后续他计划继续分享相关学习心得。

最后，作者推荐大家尝试他做的本地 Skill：

- GitHub 项目：<https://github.com/jimliu/baoyu-design>

---

## 9. 回复中的补充观点：Claude Design 需要 Design System

作者在回复中补充了一个关键前提：

> Claude Design 很重要的前提是：你要有自己的 Design System。

如果已经有成熟的设计系统，那么：

- UI 规范已经在 Design System 中定义好了；
- Claude Design 不需要从零设计大量 UI；
- 使用者可以更多关注 **UX 流程设计**，而不是 UI 细节。

作者认为，这才是使用 Claude Design 更正确的方向。

---

### 📋 提示词模板: 高价值

[Prompt: 高价值]

### 🤖 AI

# 高价值信息提取

## 1. 一句话判断

> 最值得保留的是：**AI 编程的关键不只是选模型，而是把“设计稿—代码—反馈—版本差异”全部文本化、可运行化、可追踪化，形成适合 Agent 工作的开发闭环。**

价值评级：**A**

精炼理由：文章真正有价值的不是“Claude Design 好用”，而是提出了一套更适合 AI Agent 的产品开发工作流：**设计即代码、差异即指令、运行即反馈、拆分即能力**。

---

## 2. 最重要的洞见

### 洞见 1：对 AI 来说，HTML / CSS / React / data 比 Figma 更像“设计稿”

#### **核心观点：**  
> 传统设计稿是给人看的；文本化、结构化、带数据模型的设计稿，才是给 AI 用的。

**为什么重要：**  
Figma 强在视觉表达，但 AI 更擅长处理文本、结构、组件、diff、数据关系。  
Claude Design 输出的 HTML、CSS、React、data.js，天然把设计系统、组件层级、数据结构暴露出来。

**反常识之处：**  
很多人以为 AI 应该“看懂 Figma”；但更高效的方式是：**让设计稿变成 AI 原生可读的代码资产**。

**可迁移价值：**  
凡是要让 AI 参与的流程，都应尽量从“视觉/二进制/隐式知识”转为：

- 文本化
- 结构化
- 可 diff
- 可版本管理
- 可运行

**我应该记住什么：**  
> AI 时代的交付物，不只是给人审美，更要给 Agent 执行。

---

### 洞见 2：版本拆得越小，Agent 质量越高

#### **核心观点：**  
> 使用 Coding Agent 的能力，不只是会写 prompt，而是会把任务拆成它能稳定完成的小版本。

**为什么重要：**  
作者强调第一版只实现 UI，功能越少，Agent 越容易聚焦，结果越好。  
这本质上是降低搜索空间：任务越窄，模型越不容易发散、遗漏、幻觉。

**反常识之处：**  
很多人希望一次性让 Agent 做完整产品；但真正有效的方式是：**让 Agent 每次只处理一个明确、可验证的小闭环**。

**可迁移价值：**  
适用于所有 AI 协作场景：

- 写代码：先 UI，再功能，再性能
- 写文章：先结构，再段落，再润色
- 做产品：先核心路径，再边界情况
- 做策略：先假设，再验证，再扩展

**我应该记住什么：**  
> 拆任务不是项目管理细节，而是 AI 协作的核心能力。

---

### 洞见 3：设计变更不要直接改代码，而要先改“源头设计稿”

#### **核心观点：**  
> UI 有问题，不应让 Agent 直接在代码里修补，而应回到设计稿修改，再让 Agent 根据 git diff 同步实现。

**为什么重要：**  
这避免了代码和设计逐渐漂移。  
设计稿是“事实源”，代码是实现结果；如果直接改代码，长期会失去一致性。

**反常识之处：**  
很多 AI 开发会进入“截图—指出问题—让模型直接修代码”的循环，看似快，实则容易制造混乱。  
作者的做法更像工程化流程：**先更新设计源，再由 diff 驱动实现变更**。

**可迁移价值：**  
任何系统都要维护“单一事实源”：

- 产品需求不要散落在聊天记录里
- 设计规范不要散落在代码补丁里
- 数据口径不要散落在报表里
- 组织决策不要散落在会议口头表达里

**我应该记住什么：**  
> AI 可以加速执行，但不能替代事实源管理。

---

### 洞见 4：即时可运行反馈，是人和 Agent 共同校准方向的基础

#### **核心观点：**  
> 每个版本都必须能运行；没有运行反馈，Agent 和人都无法判断下一步。

**为什么重要：**  
AI 开发不是一次生成，而是反馈驱动的连续校准。  
可运行版本提供了真实约束：界面是否对、交互是否顺、性能是否撑得住。

**可迁移价值：**  
这是一条通用决策原则：  
> 不要长时间停留在抽象讨论里，要尽快制造可观察反馈。

适用于产品、组织、个人成长：

- 不要写半年方案，先做可测试原型
- 不要争论战略，先找真实用户反馈
- 不要幻想能力提升，先做可验证作品

**我应该记住什么：**  
> 反馈不是开发后的验收，而是开发过程本身。

---

### 洞见 5：Design System 让 AI 从“画 UI”转向“设计 UX”

#### **核心观点：**  
> Claude Design 的重要前提是已有 Design System；有了 DS，AI 不需要重新发明 UI，而能更多聚焦 UX。

**为什么重要：**  
Design System 把颜色、尺寸、组件、规范提前固化，减少 AI 的自由发挥空间。  
AI 最怕无约束创作，最适合在清晰约束内组合、生成、迭代。

**反常识之处：**  
很多人以为 AI 设计能力强，就不需要设计系统；实际相反：  
> 越想让 AI 稳定产出，越需要强约束的设计系统。

**可迁移价值：**  
AI 提效的前提通常不是“更自由”，而是“更规范”：

- 代码有架构规范，Agent 才写得稳
- 内容有风格指南，AI 才写得一致
- 销售有话术框架，AI 才能规模化
- 组织有流程标准，AI 才能自动化

**我应该记住什么：**  
> AI 放大的是系统能力，不是混乱中的灵感。

---

## 3. 底层逻辑

#### **逻辑一：AI 协作效率取决于交付物是否 Agent-friendly**

- 表层现象：Claude Design 输出 HTML/CSS/React/data，比 Figma 更适合后续开发。
- 深层原因：AI 更擅长处理文本、结构、差异、代码，而不是隐式视觉意图。
- 推导结论：未来高效工作流会倾向于把中间产物变成可读、可 diff、可执行的文本资产。

---

#### **逻辑二：Agent 的能力边界要求任务被压缩到可控范围**

- 表层现象：作者不让 Agent 一次性实现太多功能，而是先做 UI，再逐步补功能。
- 深层原因：任务越大，模型上下文压力、决策分支和错误概率越高。
- 推导结论：会用 AI 的关键不是“提出大目标”，而是“把大目标切成连续可验证的小闭环”。

---

#### **逻辑三：稳定迭代依赖“事实源 + 差异同步 + 运行反馈”**

- 表层现象：设计稿修改后，用 git diff 让 Agent 理解变化，再根据截图和运行结果调整。
- 深层原因：Agent 需要明确上下文、变化边界和反馈信号，才能避免随机修补。
- 推导结论：AI 开发的成熟形态不是聊天式生成，而是工程化闭环。

---

## 4. 反常识清单

| 反常识观点 | 常见认知 | 文章的不同判断 | 启发 |
|---|---|---|---|
| Figma 不一定是 AI 开发的最佳设计交付物 | 设计稿就应该在 Figma 里完成 | HTML/CSS/React/data 对 AI 更友好 | 面向 AI 的工作流，应优先考虑结构化、文本化、可 diff |
| Agent 不是任务越大越省事，而是任务越小越强 | 一次让 AI 做完整功能最划算 | 小版本、少功能、可运行，效果更好 | AI 协作的核心能力是拆分任务 |
| UI 问题不应直接改代码 | 看到界面问题就让 AI 修代码 | 应先修改设计稿，再让 Agent 根据 diff 更新实现 | 维护单一事实源，避免设计与代码漂移 |
| AI 设计更需要 Design System，而不是更少规范 | AI 会设计，所以规范没那么重要 | DS 越清晰，AI 越能稳定输出，越能专注 UX | 约束不是限制 AI，而是释放 AI |
| 选模型要按任务分工，而不是统一迷信一个模型 | 最强模型应该包办全部任务 | Opus 更适合 UI 实现，Codex 可处理非 UI 工作 | AI 工作流应做模型路由，而不是单模型崇拜 |

---

