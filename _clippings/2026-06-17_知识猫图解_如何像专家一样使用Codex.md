---
title: "2026-06-17_GeekCatX_如何像专家一样使用Codex"
source: "https://x.com/GeekCatX/status/2061373240258470154"
author:
  - "[[@GeekCatX]]"
published: 2026-06-17
created: 2026-06-17
description:
tags:
  - "x"
  - "@GeekCatX"
  - "codex"
  - "openai"
---

# 如何像专家一样使用Codex

**知识猫图解**

# 如何像专家一样使用Codex

像专家一样使用 Codex，不是靠“神奇提示词”，而是建立一套可复用、可验证、可控权限、可沉淀经验的工程工作流。

一句话：让 Codex 做执行、探索、验证、整理；你负责目标、边界、验收和最终判断。

# 1\. 专家不会直接说“帮我写代码”

新手提示词：

> 帮我做一个登录功能。

专家提示词：

> 目标： 为现有项目增加邮箱+密码登录功能。 范围： - 前端：登录页、表单校验、错误提示 - 后端：登录 API - 数据库：如有必要新增用户表字段 - 不要修改现有注册流程 约束： - 不新增大型依赖 - 保持现有 UI 风格 - 不要直接提交 commit 验收标准： 1. 空邮箱、错误邮箱、错误密码都有提示 2. 登录成功后跳转到 dashboard 3. 登录失败不泄露用户是否存在 4. 添加必要测试 5. npm test / lint / typecheck 通过 工作方式： 先阅读代码并给实现计划，不要马上修改。

专家的关键是：任务必须有目标、范围、约束、验收标准、执行流程。

# 2\. 第一件事：让 Codex 先读项目，不要先改项目

Codex CLI 能在本地终端读取、修改并运行所选目录里的代码。也正因为它能动真实项目，所以专家第一步通常是“只读分析”。(

[OpenAI 開發者](https://developers.openai.com/codex/cli?utm_source=chatgpt.com))

你每进一个新项目，先发：

> 请只阅读项目，不要修改任何文件。 输出： 1. 项目是做什么的 2. 技术栈 3. 目录结构 4. 关键业务流程 5. 启动、测试、构建命令 6. 最容易出问题的模块 7. 建议写入 AGENTS.md 的项目规则

然后再问：

> 请找出和【某功能】相关的所有文件，并画出调用链。 不要修改代码。

这能防止 Codex 一上来乱改。

# 3\. 必须维护 AGENTS.md

AGENTS.md 是 Codex 专家级使用的核心。官方文档说明，Codex 会在开始工作前读取 AGENTS.md，可以通过全局指导和项目级覆盖，让每次任务都有一致的期望。(

[OpenAI 開發者](https://developers.openai.com/codex/guides/agents-md?utm_source=chatgpt.com))

先运行：

> /init

官方最佳实践也提到，/init 可以快速生成初始 AGENTS.md，但你应该手动编辑，让它符合团队真实的构建、测试、review 和发布流程。(

[OpenAI 開發者](https://developers.openai.com/codex/learn/best-practices?utm_source=chatgpt.com))

推荐你的 AGENTS.md 这样写：

```markdown
# AGENTS.md

## 项目说明
这是一个 Next.js + TypeScript 项目，用于 xxx。

## 常用命令
- 安装依赖：npm install
- 本地启动：npm run dev
- 类型检查：npm run typecheck
- Lint：npm run lint
- 测试：npm test
- 构建：npm run build

## 工作规则
- 修改前必须先说明影响范围和计划
- 默认最小改动
- 不要无理由新增依赖
- 不要重写无关模块
- 改业务逻辑必须补测试
- 改 UI 必须检查 loading、empty、error、mobile 状态
- 完成后必须说明测试结果和潜在风险

## 完成标准
- 相关测试通过
- lint/typecheck/build 通过
- 行为符合验收标准
- diff 可 review
```

高级技巧：每次 Codex 犯错，就把规则补进 AGENTS.md。

例如它经常乱加依赖，你就加：

> 禁止新增依赖，除非先说明原因、替代方案和体积影响，并获得确认。

# 4\. 用 /plan 控制复杂任务

专家不会让 Codex 直接做大任务，而是先规划。

> /plan 我要把当前项目的支付流程重构成更清晰的 service 层。 请先分析影响范围、风险、迁移步骤和测试策略。 不要修改代码。

Codex App 的命令说明里，/plan 用于多步骤规划，/goal 用于设置持续目标，官方也建议先用 /plan 塑形再用 /goal。(

[OpenAI 開發者](https://developers.openai.com/codex/app/commands?utm_source=chatgpt.com))

你可以要求它输出：

> 请按这个格式给计划： 1. 你理解的目标 2. 涉及文件 3. 风险点 4. 分阶段方案 5. 每阶段验收标准 6. 需要我确认的问题

专家习惯是：计划没通过，不让它改代码。

# 5\. 用 /goal 处理长任务

当任务不是一次就能完成，比如迁移、重构、修复大量测试，可以用 /goal。官方说明 /goal 适合让 Codex 围绕一个可验证的停止条件持续工作。(

[OpenAI 開發者](https://developers.openai.com/codex/use-cases/follow-goals?utm_source=chatgpt.com))

示例：

> /goal Complete the migration from local state to Zustand. Stop only when: 1. App behavior is unchanged 2. All tests pass 3. npm run build succeeds 4. State management code is documented 5. A migration summary is written

适合：

> 修复所有 failing tests 完成技术栈迁移 重构大型模块 补齐测试覆盖 完成 MVP 清理技术债

重点是：停止条件必须具体可验证。

# 6\. 权限要分模式，不要一直全开放

Codex 的 sandbox 是让它能自主行动但不获得无限制机器权限的边界；它运行本地命令时会在受限环境中执行，而不是默认拥有完整系统权限。(

[OpenAI 開發者](https://developers.openai.com/codex/concepts/sandboxing?utm_source=chatgpt.com))

专家常用 3 种模式。

## 只读分析模式

适合：读代码、审查、解释、规划。

> /permissions 切换到 read-only。

然后：

> 请分析当前项目的认证流程，不要修改代码。

## 常规开发模式

适合：日常写代码、跑测试。

> codex --sandbox workspace-write --ask-for-approval on-request

这个模式下，Codex 可以在工作区内读写和运行命令，但越界操作需要请求批准。官方资料也说明 Auto 预设类似 workspace-write + on-request，会在编辑工作区外文件或需要网络访问时请求批准。(

[OpenAI 開發者](https://developers.openai.com/codex/llms-full.txt?utm_source=chatgpt.com))

## 高风险操作模式

凡是涉及这些事，都要明确限制：

> 删除文件 数据库迁移 生产配置 密钥 支付 认证权限 CI/CD 部署 批量重构

你可以说：

> 这是高风险任务。 只允许分析和提出方案，不允许修改文件、运行迁移、删除数据或改配置。

# 7\. 每次都让它跑测试，而不是只看它说“完成了”

专家级工作流：

> 请完成修改后运行： 1. npm run lint 2. npm run typecheck 3. npm test 4. npm run build 如果失败： - 先解释失败原因 - 再修复 - 不要掩盖测试 - 不要删除测试

再加一句非常重要：

> 如果无法运行测试，请明确说明原因，并告诉我应该手动运行哪些命令。

Codex 专家不会相信“看起来可以”，只相信可复现验证结果。

# 8\. 用 /review 当第二双眼睛

写完代码后，不要立刻合并。让 Codex 进入 review 模式。

> /review 请 review 当前未提交改动，重点检查： 1. 逻辑 bug 2. 安全风险 3. 边界条件 4. 测试缺失 5. 过度设计 6. 是否违反 AGENTS.md

官方命令说明中，/review 可用于审查未提交改动或和 base branch 比较。(

[OpenAI 開發者](https://developers.openai.com/codex/app/commands?utm_source=chatgpt.com))

更强的用法：

> 请不要修改代码，只输出 review 结果。 按 P0 / P1 / P2 分类。 每个问题包含： - 问题描述 - 相关文件 - 为什么有风险 - 建议修复方式

# 9\. 用 Subagents 做并行专家审查

Subagents 是高级功能。官方说明 Codex 可以生成专门的子 agent 并行探索、处理或分析任务，最后汇总结果。(

[OpenAI 開發者](https://developers.openai.com/codex/subagents?utm_source=chatgpt.com))

你可以这样用：

> 请使用 subagents 并行审查当前改动： Agent A：检查业务逻辑和边界条件 Agent B：检查安全、认证、权限问题 Agent C：检查测试覆盖 Agent D：检查性能和可维护性 Agent E：检查 UI/UX、移动端、可访问性 不要修改代码。 最后合并成一份 review 报告，按严重程度排序。

适合：

上线前审查 大型重构后检查 安全敏感功能 复杂 bug 定位 多模块改动

# 10\. 用 Skill 封装重复流程

Skills 是 Codex 高阶能力之一。官方说明，Skill 是由说明、资源和可选脚本组成的任务能力包，可以让 Codex 更可靠地执行特定工作流。(

[OpenAI 開發者](https://developers.openai.com/codex/skills?utm_source=chatgpt.com))

也就是说，你不应该每次都写长提示词，而是把常用流程封装成 Skill。

例如你创建一个：

> frontend-polish

SKILL.md 可以写：

```markdown
---
name: frontend-polish
description: Use this skill when improving UI quality, layout, responsiveness, accessibility, loading states, empty states, and error states.
---

When triggered:
1. Inspect affected components
2. Check layout, spacing, typography, and responsive behavior
3. Ensure loading, empty, and error states exist
4. Check accessibility labels and keyboard navigation
5. Avoid changing unrelated business logic
6. Run typecheck/build if available
7. Summarize visual and behavior changes
```

调用：

> [$frontend-polish](https://x.com/search?q=%24frontend-polish&src=cashtag_click) 请优化当前 dashboard 页面，但不要改业务逻辑。

官方变更日志也提到，Skills 可以在 Codex CLI 和 IDE 扩展中使用，可显式输入

[$skill-name](https://x.com/search?q=%24skill-name&src=cashtag_click) 调用，也可以让 Codex 根据提示自动选择。(

[OpenAI 開發者](https://developers.openai.com/codex/changelog?type=codex-cli&utm_source=chatgpt.com)

)

# 11\. 用自定义 Slash Command 做快捷工作流

Codex CLI 的 slash commands 可以快速切换模型、调整权限、总结长对话，也能创建自定义命令。(

[OpenAI 開發者](https://developers.openai.com/codex/cli/slash-commands?utm_source=chatgpt.com))

你可以创建这些命令：

> /deep-review /fix-tests /write-readme /explain-module /refactor-safely /security-audit /frontend-polish

例如 /deep-review 的内容：

> 请深度 review 当前 diff。 检查： 1. correctness 2. edge cases 3. security 4. performance 5. tests 6. maintainability 7. whether it follows AGENTS.md 不要修改代码。 按 P0/P1/P2 输出。

以后只要输入：

> /deep-review

就能复用整套审查流程。

# 12\. 用 MCP 连接外部上下文

Codex 的 customization 层包括项目指导、Skills、MCP 和 Subagents。官方说明，自定义就是让 Codex 按你的团队工作方式工作。(

[OpenAI 開發者](https://developers.openai.com/codex/concepts/customization?utm_source=chatgpt.com))

MCP 的价值是：让 Codex 不只看本地代码，还能接入外部系统。

例如：

> GitHub：读 issue、PR、CI 结果 Linear/Jira：读任务和验收标准 Figma：读设计稿 Sentry：读线上错误 Notion：读产品文档 Docs：查内部 API 文档

专家用法：

> 请读取这个 Linear ticket 的验收标准，再检查当前代码是否满足。 如果不满足，先给实现计划，不要修改代码。

或者：

> 请根据 Sentry 错误日志定位问题。 要求： 1. 找到可能的代码位置 2. 给复现路径 3. 判断影响范围 4. 提出最小修复方案 5. 不要直接改代码

# 13\. 长会话要定期压缩上下文

长任务最容易跑偏。Codex App 的 /status 可以显示 thread ID、上下文使用量和速率限制。(

[OpenAI 開發者](https://developers.openai.com/codex/app/commands?utm_source=chatgpt.com))

当任务做了很久，你可以说：

> /status

然后：

> 请总结当前任务状态： 1. 原始目标 2. 已做改动 3. 已确认的决策 4. 失败过的尝试 5. 当前剩余问题 6. 下一步计划 7. 需要继续遵守的约束 后续请基于这个摘要继续。

专家会主动“整理战场”，避免 Codex 忘记前面的约束。

# 14\. 专家常用的 8 个提示词模板

## A. 读项目

请只读项目，不要修改文件。 输出： 1. 项目目标 2. 技术栈 3. 目录结构 4. 核心数据流 5. 关键文件 6. 启动/测试/构建命令 7. 潜在风险

## B. 做功能

请实现【功能】。 范围： 【允许修改的模块】 约束： 【不要改什么】 验收标准： 1. 2. 3. 流程： 先给计划，不要马上改。 确认计划后再实现。 实现后运行测试并总结。

## C. 修 bug

请修复这个 bug。 要求： 1. 先复现或解释如何复现 2. 找根因 3. 给最小修复方案 4. 修改最少代码 5. 补充回归测试 6. 跑测试 7. 总结风险

## D. 重构

请重构这个模块。 目标： 提升可读性和可维护性。 约束： 1. 外部行为不变 2. API 不变 3. 不新增依赖 4. 每一步都保持测试通过 请先列出重构计划和风险。

## E. 写测试

请为【模块/功能】补测试。 覆盖： 1. 正常路径 2. 边界条件 3. 错误输入 4. 权限问题 5. 回归场景 不要为了让测试通过而修改业务逻辑，除非发现真实 bug。

## F. Review

请 review 当前 diff，不要修改代码。 重点： 1. 逻辑 bug 2. 安全问题 3. 边界条件 4. 性能问题 5. 缺失测试 6. 过度复杂 7. 是否违反 AGENTS.md 按严重程度输出。

## G. 写文档

请根据当前项目生成 README。 包括： 1. 项目介绍 2. 功能列表 3. 技术栈 4. 本地运行 5. 环境变量 6. 测试命令 7. 部署方式 8. 常见问题

## H. 复盘沉淀

请总结这次任务中可以沉淀到 AGENTS.md 的规则。 输出： 1. 新增规则 2. 为什么需要 3. 应放在哪个章节 4. 是否会影响未来任务

# 15\. 专家级 Codex 工作流

每次任务都按这个顺序：

1\. 明确目标 2. 明确范围 3. 明确不做什么 4. 明确验收标准 5. 让 Codex 只读分析 6. 让 Codex 给计划 7. 人类确认计划 8. 小步实现 9. 跑测试 10. Review diff 11. 修复 review 问题 12. 更新文档 13. 沉淀规则到 AGENTS.md / Skill

你可以直接用这个总控提示词：

```markdown
你是我的 Codex 专家级编程搭档。

工作规则：
1. 不要急着写代码
2. 先阅读相关文件
3. 先说明你理解的目标、范围、风险
4. 给出计划后等待确认
5. 修改时保持最小改动
6. 不要无理由新增依赖
7. 修改后必须运行相关测试
8. 如果测试无法运行，说明原因和手动命令
9. 最后输出改动摘要、测试结果、潜在风险
10. 发现可复用经验时，建议写入 AGENTS.md 或 Skill

当前任务：
【写你的任务】

验收标准：
【写怎样算完成】
```

# 16\. 判断你是否已经“像专家一样使用 Codex”

你能做到这些，就已经不是普通用户了：

1.  不再用模糊提示词，而是写目标、范围、约束、验收标准。
2.  每个项目都有 AGENTS.md。
3.  大任务先 /plan，长任务用 /goal。
4.  会按风险切换权限，而不是一直全开放。
5.  每次改完都要求测试、lint、typecheck、build。
6.  会用 /review 和 subagents 做多角度审查。
7.  把重复流程封装成 Skill 或自定义命令。
8.  会用 MCP 接入 issue、设计稿、日志、文档等上下文。
9.  会让 Codex 复盘，并把经验沉淀。
10.  永远不盲信 Codex，最终由你验收。

最重要的心法是：

Codex 负责高强度执行，你负责工程判断。专家不是让 Codex 自由发挥，而是把 Codex 放进一套严密的工作流里。

---

<br>

## 💬 对话记录

### 📋 提示词模板: 解读

先对内容进行总结；再进行深入浅出地解读；最后给出你的意见；要求 通俗易懂，以 markdown 格式显示。内容务必精炼，再精炼。。 --reasoning_effort high

### 🤖 AI

# 一、总结

这篇文章核心讲：**高手用 Codex，不靠神奇提示词，而靠工程化流程。**

一句话：

> **Codex 负责执行、探索、验证、整理；人负责目标、边界、验收和判断。**

关键做法：

1. **任务说清楚**：目标、范围、约束、验收标准。
2. **先读项目，再改代码**：避免乱改。
3. **维护`AGENTS.md`**：把项目规则沉淀下来。
4. **大任务先`/plan`**：计划确认后再动手。
5. **长任务用`/goal`**：设置明确停止条件。
6. **权限分级**：只读、常规开发、高风险限制。
7. **必须跑测试**：lint、typecheck、test、build。
8. **用`/review`复查**：找 bug、安全、边界、测试问题。
9. **用 Subagents 多角度审查**。
10. **用 Skill / Slash Command 复用流程**。
11. **用 MCP 接入外部上下文**：Issue、设计稿、日志、文档。
12. **长会话定期总结**：防止跑偏。
13. **经验沉淀**：把错误和规则写进`AGENTS.md`或 Skill。

---

# 二、深入浅出解读

## 1. 不要把 Codex 当 “许愿机”

新手会说：

> 帮我写个登录功能。

高手会说：

> 做什么、改哪里、不改哪里、怎样算完成、先别动代码，先给计划。

本质区别是：

| 新手 | 高手 |
|---|---|
| 给模糊愿望 | 给工程任务 |
| 让 AI 自由发挥 | 给 AI 明确边界 |
| 看结果像不像 | 用测试和验收判断 |
| 一次性使用 | 沉淀成规则 |

---

## 2. `AGENTS.md`是 “项目说明书”

它的作用类似给 Codex 的团队规范：

- 项目是什么
- 用什么技术
- 怎么启动
- 怎么测试
- 什么不能做
- 完成标准是什么

每次 Codex 犯错，都把规则补进去。

比如：

> 禁止新增依赖，除非先说明原因、替代方案和影响，并获得确认。

这样 Codex 会越来越贴合你的项目习惯。

---

## 3. 大任务不要直接开干

复杂任务应该这样：

```text
先分析
再计划
我确认
再实现
再测试
再 review
最后总结
```

也就是：

> **先控方向，再让 Codex 执行。**

否则 AI 很容易：

- 改太多
- 引入新依赖
- 重写无关模块
- 忽略测试
- 破坏原有逻辑

---

## 4. 测试比 “它说完成了” 更重要

Codex 说完成，不等于真的完成。

高手会要求：

```bash
npm run lint
npm run typecheck
npm test
npm run build
```

如果跑不了，也必须说明原因和手动命令。

核心原则：

> **不要相信描述，要相信可复现结果。**

---

## 5. 权限必须控制

Codex 能读写本地代码，所以不能一直全开放。

建议：

| 场景 | 权限 |
|---|---|
| 看项目、分析、规划 | 只读 |
| 日常开发 | 工作区可写，越界需批准 |
| 数据库、支付、认证、部署 | 只分析，不允许直接改 |

本质是：

> AI 可以很强，但不能无边界。

---

# 三、我的意见

这篇文章的价值很高，核心不是教 “提示词”，而是教**AI 编程管理方法**。

我认为最值得立刻实践的是这 5 件事：

## 1. 每个项目先建`AGENTS.md`

这是最低成本、最高收益的动作。

## 2. 所有任务都写清楚四件事

```text
目标
范围
约束
验收标准
```

## 3. 默认先让 Codex 只读分析

不要上来就让它改代码。

## 4. 改完必须测试

没有测试结果，就不算完成。

## 5. 把常用流程做成模板

比如：

- 修 bug 模板
- review 模板
- 写测试模板
- 重构模板
- 写文档模板

---

# 最精简心法

> **不要让 Codex 自由发挥。**  
> **把它放进流程里，让它高强度执行。**  
> **最终判断权永远在人手里。**

---

### 📋 提示词模板: 通俗精炼

我是一名资深产品经理，对科技、ai、商业、产品、社会等有深刻理解。
请帮我对文章内容进行通俗易懂的总结，返回内容要精炼简洁，、层次结构必须清晰，有标题，有段落（含段落标题），markdown 格式。

### 🤖 AI

# 如何像专家一样使用 Codex：核心总结

## 一句话结论

像专家一样使用 Codex，关键不是写 “神奇提示词”，而是建立一套**目标清晰、权限可控、过程可验证、经验可沉淀**的工程工作流。

> Codex 负责执行、探索、验证和整理；人负责目标、边界、验收和最终判断。

---

## 1. 专家提示词的本质：把任务工程化

新手会说：“帮我写一个登录功能。”

专家会明确：

- **目标**：要实现什么
- **范围**：允许改哪些模块
- **约束**：不能做什么
- **验收标准**：怎样算完成
- **执行流程**：先分析、再计划、确认后再改

核心原则是：  
**不要让 Codex 猜需求，要把任务拆成可执行、可验证的工程指令。**

---

## 2. 先读项目，再改代码

进入新项目时，不要让 Codex 直接动手。

应先要求它只读分析：

- 项目做什么
- 技术栈是什么
- 目录结构如何
- 核心业务流程在哪里
- 启动、测试、构建命令是什么
- 哪些模块最容易出问题

这样可以避免 Codex 一上来乱改代码，尤其适合接手陌生项目或复杂老项目。

---

## 3. AGENTS.md 是长期协作的 “项目规则书”

AGENTS.md 是 Codex 的项目级工作规范。它应该记录：

- 项目说明
- 常用命令
- 编码规则
- 测试要求
- Review 标准
- 不允许做的事情
- 完成标准

高级用法是：  
**每次 Codex 犯错，就把规则补进 AGENTS.md。**

例如：

> 禁止新增依赖，除非先说明原因、替代方案和体积影响，并获得确认。

这相当于把团队经验沉淀为 Codex 的长期行为约束。

---

## 4. 大任务先`/plan`，长任务用`/goal`

复杂任务不要直接执行，要先规划。

`/plan`适合：

- 重构
- 迁移
- 复杂功能开发
- 风险评估
- 多阶段实施

要求 Codex 输出：

1. 它理解的目标
2. 涉及文件
3. 风险点
4. 分阶段方案
5. 每阶段验收标准
6. 需要确认的问题

长周期任务则用`/goal`，并设置明确停止条件，例如：

- 所有测试通过
- 构建成功
- 行为不变
- 文档补齐
- 迁移总结完成

重点是：  
**计划没通过，不让它改代码。**

---

## 5. 权限管理：不要一直全开放

Codex 能操作真实项目，所以必须按风险控制权限。

常见三种模式：

### 只读分析模式

适合：

- 读代码
- 审查
- 解释逻辑
- 制定计划

要求：不允许修改任何文件。

### 常规开发模式

适合日常开发，可允许在工作区内读写和运行命令，但越界操作需要审批。

### 高风险任务模式

涉及以下内容时必须严格限制：

- 删除文件
- 数据库迁移
- 生产配置
- 密钥
- 支付
- 认证权限
- CI/CD
- 部署
- 大规模重构

原则是：  
**风险越高，权限越小，确认越多。**

---

## 6. 不相信 “完成了”，只相信可验证结果

专家不会只看 Codex 说 “完成了”。

每次修改后，都应要求运行：

- `npm run lint`
- `npm run typecheck`
- `npm test`
- `npm run build`

如果无法运行测试，必须说明原因，并给出人工执行命令。

核心判断标准是：  
**不是看起来能用，而是可复现地通过验证。**

---

## 7. 用`/review`和 Subagents 做第二轮审查

代码写完后，不要急着合并。

可以用`/review`检查：

- 逻辑 bug
- 安全风险
- 边界条件
- 测试缺失
- 性能问题
- 过度设计
- 是否违反 AGENTS.md

更复杂场景可用 Subagents 并行审查：

- 业务逻辑 Agent
- 安全权限 Agent
- 测试覆盖 Agent
- 性能可维护性 Agent
- UI/UX Agent

适合上线前、大型重构后、安全敏感功能等场景。

---

## 8. 把重复流程封装成 Skill 或自定义命令

专家不会每次重复写长提示词，而是把常用流程产品化。

例如封装：

- `frontend-polish`
- `deep-review`
- `fix-tests`
- `security-audit`
- `write-readme`
- `refactor-safely`

这样 Codex 就能按固定流程执行，减少随机性，提高稳定性。

本质是：  
**把一次性提示词升级为可复用工作流。**

---

## 9. 用 MCP 接入外部上下文

Codex 不应只看本地代码，还可以通过 MCP 接入更多上下文：

- GitHub Issue / PR / CI
- Linear / Jira 任务
- Figma 设计稿
- Sentry 错误日志
- Notion 产品文档
- 内部 API 文档

典型用法：

> 读取 Linear ticket 的验收标准，再检查当前代码是否满足；如果不满足，先给计划，不要改代码。

这能让 Codex 从 “代码助手” 升级为 “工程上下文助手”。

---

## 10. 长任务要定期压缩上下文

长会话容易跑偏，需要定期整理当前状态：

- 原始目标
- 已做改动
- 已确认决策
- 失败过的尝试
- 剩余问题
- 下一步计划
- 仍需遵守的约束

这相当于给 Codex 做 “项目中场复盘”，避免它遗忘前文约束。

---

## 11. 专家级 Codex 工作流

完整流程可以概括为：

1. 明确目标
2. 明确范围
3. 明确不做什么
4. 明确验收标准
5. 让 Codex 只读分析
6. 让 Codex 制定计划
7. 人类确认计划
8. 小步实现
9. 运行测试
10. Review diff
11. 修复 review 问题
12. 更新文档
13. 沉淀规则到 AGENTS.md/ Skill

---

## 12. 最重要的心法

专家不是让 Codex 自由发挥，而是把它放进一套严密的工程系统里。

Codex 的价值在于：

- 高强度执行
- 快速探索
- 自动验证
- 整理总结

人的价值在于：

- 定义目标
- 设置边界
- 控制风险
- 判断取舍
- 最终验收

> 真正的专家级用法是：让 AI 做执行，把工程判断牢牢掌握在人手里。

---

### 📋 提示词模板: 结构化

请以结构化方式并用通俗易懂的语言 精炼总结 文章内容，如果需要可以举具体的例子，并用中文返回。
要求总结结构如下：   
- 开头用1-2句话概括主旨,   
- 按原文逻辑分段组织内容,   
- 每段内容提炼关键点，用简练语言表达,  
- 保留原文中的重要数据和案例.   
，以 markdown 格式显示。
   --reasoning_effort high

### 🤖 AI

# 如何像专家一样使用 Codex：核心总结

像专家一样使用 Codex，关键不在 “神奇提示词”，而在建立一套**可复用、可验证、权限可控、经验可沉淀的工程工作流**。  
简单说：**Codex 负责执行、探索、验证和整理；人负责目标、边界、验收和最终判断。**

---

## 1. 不要只说 “帮我写代码”，要把任务定义清楚

新手常用模糊提示词，例如：

> 帮我做一个登录功能。

专家会明确说明：

- **目标**：为现有项目增加邮箱 + 密码登录
- **范围**：
  - 前端：登录页、表单校验、错误提示
  - 后端：登录 API
  - 数据库：必要时新增字段
- **约束**：
  - 不新增大型依赖
  - 保持现有 UI 风格
  - 不直接提交 commit
- **验收标准**：
  1. 空邮箱、错误邮箱、错误密码都有提示
  2. 登录成功跳转到 dashboard
  3. 登录失败不泄露用户是否存在
  4. 添加必要测试
  5. `npm test /lint/typecheck`通过
- **执行流程**：先阅读代码并给计划，不要马上修改

**关键点**：专家提示词必须包含目标、范围、约束、验收标准和执行流程。

---

## 2. 进入新项目后，先让 Codex 只读分析

Codex CLI 可以读取、修改并运行本地项目代码，因此不能一上来就让它改文件。

推荐先输入：

```text
请只阅读项目，不要修改任何文件。
输出：
1. 项目是做什么的
2. 技术栈
3. 目录结构
4. 关键业务流程
5. 启动、测试、构建命令
6. 最容易出问题的模块
7. 建议写入 AGENTS.md 的项目规则
```

然后再问：

```text
请找出和【某功能】相关的所有文件，并画出调用链。
不要修改代码。
```

这样可以防止 Codex 在不了解项目的情况下乱改代码。

---

## 3. 必须维护 AGENTS.md，让规则长期生效

`AGENTS.md`是 Codex 专家级使用的核心文件。Codex 开始工作前会读取它，用来理解项目规则、测试流程和团队规范。

可以先运行：

```text
/init
```

生成初始`AGENTS.md`，再手动补充真实规则。

推荐内容包括：

```markdown
# AGENTS.md

## 项目说明
这是一个 Next.js + TypeScript 项目，用于 xxx。

## 常用命令
- 安装依赖：npm install
- 本地启动：npm run dev
- 类型检查：npm run typecheck
- Lint：npm run lint
- 测试：npm test
- 构建：npm run build

## 工作规则
- 修改前必须先说明影响范围和计划
- 默认最小改动
- 不要无理由新增依赖
- 不要重写无关模块
- 改业务逻辑必须补测试
- 改 UI 必须检查 loading、empty、error、mobile 状态
- 完成后必须说明测试结果和潜在风险

## 完成标准
- 相关测试通过
- lint/typecheck/build 通过
- 行为符合验收标准
- diff 可 review
```

高级技巧：**Codex 每犯一次错，就把对应规则补进 AGENTS.md**。

例如它经常乱加依赖，就加：

```text
禁止新增依赖，除非先说明原因、替代方案和体积影响，并获得确认。
```

---

## 4. 复杂任务先用`/plan`，不要直接开干

专家不会让 Codex 直接执行大型任务，而是先规划。

示例：

```text
/plan
我要把当前项目的支付流程重构成更清晰的 service 层。
请先分析影响范围、风险、迁移步骤和测试策略。
不要修改代码。
```

可要求计划按以下格式输出：

1. 你理解的目标
2. 涉及文件
3. 风险点
4. 分阶段方案
5. 每阶段验收标准
6. 需要我确认的问题

**原则**：计划没有通过，就不允许 Codex 改代码。

---

## 5. 长任务用`/goal`，并设置明确停止条件

当任务无法一次完成，例如迁移、重构、修复大量测试，可以使用`/goal`。

示例：

```text
/goal Complete the migration from local state to Zustand. Stop only when:
1. App behavior is unchanged
2. All tests pass
3. npm run build succeeds
4. State management code is documented
5. A migration summary is written
```

适合场景包括：

- 修复所有失败测试
- 完成技术栈迁移
- 重构大型模块
- 补齐测试覆盖
- 完成 MVP
- 清理技术债

重点是：**停止条件必须具体、可验证**。

---

## 6. 权限要分模式，不要一直全开放

Codex 的 sandbox 可以限制它的系统权限。专家会根据风险切换权限。

### 只读分析模式

适合读代码、审查、解释和规划。

```text
/permissions
切换到 read-only。
```

然后：

```text
请分析当前项目的认证流程，不要修改代码。
```

### 常规开发模式

适合日常写代码、跑测试：

```bash
codex --sandbox workspace-write --ask-for-approval on-request
```

该模式下，Codex 可以在工作区内读写和运行命令，但越界操作需要请求批准。

### 高风险操作模式

涉及以下内容时必须限制权限：

- 删除文件
- 数据库迁移
- 生产配置
- 密钥
- 支付
- 认证权限
- CI/CD
- 部署
- 批量重构

可这样提示：

```text
这是高风险任务。
只允许分析和提出方案，不允许修改文件、运行迁移、删除数据或改配置。
```

---

## 7. 每次修改后必须跑测试，而不是相信 “完成了”

专家级工作流会要求 Codex 修改后运行：

```text
1. npm run lint
2. npm run typecheck
3. npm test
4. npm run build
```

如果失败：

- 先解释失败原因
- 再修复
- 不要掩盖测试
- 不要删除测试

还要补一句：

```text
如果无法运行测试，请明确说明原因，并告诉我应该手动运行哪些命令。
```

核心原则：**不相信 “看起来可以”，只相信可复现的验证结果。**

---

## 8. 用`/review`做第二双眼睛

代码写完后，不要立刻合并，可以让 Codex 审查当前改动。

```text
/review
请 review 当前未提交改动，重点检查：
1. 逻辑 bug
2. 安全风险
3. 边界条件
4. 测试缺失
5. 过度设计
6. 是否违反 AGENTS.md
```

更强用法：

```text
请不要修改代码，只输出 review 结果。
按 P0 / P1 / P2 分类。
每个问题包含：
- 问题描述
- 相关文件
- 为什么有风险
- 建议修复方式
```

---

## 9. 用 Subagents 做并行专家审查

Subagents 可以让多个子 agent 并行审查不同维度，最后汇总结果。

示例：

```text
请使用 subagents 并行审查当前改动：

Agent A：检查业务逻辑和边界条件
Agent B：检查安全、认证、权限问题
Agent C：检查测试覆盖
Agent D：检查性能和可维护性
Agent E：检查 UI/UX、移动端、可访问性

不要修改代码。
最后合并成一份 review 报告，按严重程度排序。
```

适合：

- 上线前审查
- 大型重构后检查
- 安全敏感功能
- 复杂 bug 定位
- 多模块改动

---

## 10. 用 Skill 封装重复流程

Skill 可以把常用流程封装起来，避免每次写长提示词。

例如创建一个`frontend-polish` Skill：

```markdown
---
name: frontend-polish
description: Use this skill when improving UI quality, layout, responsiveness, accessibility, loading states, empty states, and error states.
---

When triggered:
1. Inspect affected components
2. Check layout, spacing, typography, and responsive behavior
3. Ensure loading, empty, and error states exist
4. Check accessibility labels and keyboard navigation
5. Avoid changing unrelated business logic
6. Run typecheck/build if available
7. Summarize visual and behavior changes
```

调用方式：

```text
$frontend-polish 请优化当前 dashboard 页面，但不要改业务逻辑。
```

---

## 11. 用自定义 Slash Command 做快捷工作流

Codex CLI 支持自定义 slash command，可以把常用工作流变成快捷命令。

例如：

- `/deep-review`
- `/fix-tests`
- `/write-readme`
- `/explain-module`
- `/refactor-safely`
- `/security-audit`
- `/frontend-polish`

`/deep-review`示例：

```text
请深度 review 当前 diff。

检查：
1. correctness
2. edge cases
3. security
4. performance
5. tests
6. maintainability
7. whether it follows AGENTS.md

不要修改代码。
按 P0/P1/P2 输出。
```

以后只需输入：

```text
/deep-review
```

即可复用整套审查流程。

---

## 12. 用 MCP 连接外部上下文

MCP 可以让 Codex 不只看本地代码，还能读取外部系统信息。

可接入：

- GitHub：issue、PR、CI 结果
- Linear / Jira：任务和验收标准
- Figma：设计稿
- Sentry：线上错误
- Notion：产品文档
- Docs：内部 API 文档

示例：

```text
请读取这个 Linear ticket 的验收标准，再检查当前代码是否满足。
如果不满足，先给实现计划，不要修改代码。
```

或：

```text
请根据 Sentry 错误日志定位问题。
要求：
1. 找到可能的代码位置
2. 给复现路径
3. 判断影响范围
4. 提出最小修复方案
5. 不要直接改代码
```

---

## 13. 长会话要定期压缩上下文

长任务容易跑偏，可以用`/status`查看线程 ID、上下文使用量和速率限制。

然后让 Codex 总结当前状态：

```text
请总结当前任务状态：
1. 原始目标
2. 已做改动
3. 已确认的决策
4. 失败过的尝试
5. 当前剩余问题
6. 下一步计划
7. 需要继续遵守的约束

后续请基于这个摘要继续。
```

这相当于 “整理战场”，避免 Codex 忘记前面的要求。

---

## 14. 专家常用的 8 类提示词模板

文章给出了 8 类高频模板：

1. **读项目**：只读项目，输出技术栈、目录、命令、风险等。
2. **做功能**：明确范围、约束、验收标准，先计划再实现。
3. **修 bug**：先复现、找根因、最小修复、补回归测试。
4. **重构**：保持外部行为和 API 不变，不新增依赖，分步执行。
5. **写测试**：覆盖正常路径、边界、错误输入、权限和回归场景。
6. **Review**：审查逻辑、安全、边界、性能、测试和 AGENTS.md 规则。
7. **写文档**：生成 README，包括项目介绍、运行、环境变量、部署等。
8. **复盘沉淀**：总结哪些规则应写入 AGENTS.md。

---

## 15. 专家级 Codex 标准工作流

每次任务建议按这个顺序执行：

1. 明确目标
2. 明确范围
3. 明确不做什么
4. 明确验收标准
5. 让 Codex 只读分析
6. 让 Codex 给计划
7. 人类确认计划
8. 小步实现
9. 跑测试
10. Review diff
11. 修复 review 问题
12. 更新文档
13. 沉淀规则到`AGENTS.md`或 Skill

可以使用总控提示词：

```markdown
你是我的 Codex 专家级编程搭档。

工作规则：
1. 不要急着写代码
2. 先阅读相关文件
3. 先说明你理解的目标、范围、风险
4. 给出计划后等待确认
5. 修改时保持最小改动
6. 不要无理由新增依赖
7. 修改后必须运行相关测试
8. 如果测试无法运行，说明原因和手动命令
9. 最后输出改动摘要、测试结果、潜在风险
10. 发现可复用经验时，建议写入 AGENTS.md 或 Skill

当前任务：
【写你的任务】

验收标准：
【写怎样算完成】
```

---

## 16. 判断是否已经像专家一样使用 Codex

如果你能做到以下几点，就已经不是普通用户：

- 不再用模糊提示词，而是写清目标、范围、约束、验收标准
- 每个项目都有`AGENTS.md`
- 大任务先`/plan`，长任务用`/goal`
- 按风险切换权限，而不是一直全开放
- 每次改完都要求测试、lint、typecheck、build
- 会用`/review`和 subagents 多角度审查
- 把重复流程封装成 Skill 或自定义命令
- 用 MCP 接入 issue、设计稿、日志、文档等上下文
- 让 Codex 复盘，并把经验沉淀
- 永远不盲信 Codex，最终由人验收

---

## 最重要的心法

**Codex 负责高强度执行，人负责工程判断。**

专家不是让 Codex 自由发挥，而是把它放进一套严密的工程流程中：  
先定义目标和边界，再规划、执行、测试、审查、复盘，最后把经验沉淀成规则。

---

### 📋 提示词模板: 产品-最佳实践

请根据我提供的内容，用一个具体的实例，给出在该实例之上的最佳实践，以 markdown 格式显示。    --reasoning_effort high

### 🤖 AI

# 实例：在一个 Next.js 项目中新增「邮箱 + 密码登录」功能的 Codex 最佳实践

## 一、实例背景

假设你有一个现有项目：

```text
项目：SaaS Dashboard
技术栈：
- Next.js 14
- TypeScript
- React Hook Form
- Zod
- Prisma
- PostgreSQL
- Jest / Testing Library
- ESLint
```

现在你想让 Codex 帮你新增一个登录功能。

目标不是简单地说：

> 帮我写一个登录功能。

而是建立一套**可控、可验证、可复用、可沉淀**的工作流。

---

# 二、推荐的专家级工作流

## 1. 先让 Codex 只读项目，不要马上改代码

### 提示词

```markdown
请只阅读当前项目，不要修改任何文件。

请输出：

1. 项目是做什么的
2. 技术栈
3. 目录结构
4. 当前认证相关逻辑在哪里
5. 注册流程是如何实现的
6. API 路由结构
7. 数据库模型
8. 启动、测试、构建命令
9. 新增登录功能可能影响哪些文件
10. 建议写入 AGENTS.md 的项目规则
```

### 最佳实践说明

Codex 能直接读取、修改和运行本地项目，因此第一步应该是**只读分析**。

这样可以避免它一上来就：

- 乱改文件
- 新增不必要依赖
- 重写已有模块
- 破坏已有注册流程
- 修改你不希望它碰的代码

---

## 2. 让 Codex 找出登录功能相关调用链

### 提示词

```markdown
请找出和用户认证相关的所有文件，并画出调用链。

要求：

1. 不要修改任何代码
2. 标出前端页面、表单组件、API 路由、数据库模型之间的关系
3. 标出当前是否已有注册、session、cookie、token 或 middleware 逻辑
4. 说明新增登录功能最可能复用哪些现有代码
```

### 预期输出示例

```text
认证相关文件：

- app/register/page.tsx
- components/RegisterForm.tsx
- app/api/register/route.ts
- lib/auth/password.ts
- lib/db.ts
- prisma/schema.prisma
- middleware.ts

调用链：

RegisterForm
  -> POST /api/register
    -> lib/auth/password.ts
    -> prisma.user.create ()
    -> 返回注册结果

当前暂无 login API。
当前已有 User 模型。
当前暂无 session 管理逻辑。
```

---

# 三、为这个项目维护 AGENTS.md

## 1. 先初始化

如果当前项目没有`AGENTS.md`，可以先运行：

```bash
/init
```

然后手动编辑。

---

## 2. 针对本实例的 AGENTS.md

```markdown
# AGENTS.md

## 项目说明

这是一个 Next.js + TypeScript SaaS Dashboard 项目，用于用户注册、登录和访问 dashboard。

## 技术栈

- Next.js 14
- TypeScript
- Prisma
- PostgreSQL
- React Hook Form
- Zod
- Jest
- Testing Library
- ESLint

## 常用命令

- 安装依赖：npm install
- 本地启动：npm run dev
- 类型检查：npm run typecheck
- Lint：npm run lint
- 测试：npm test
- 构建：npm run build

## 工作规则

- 修改前必须先阅读相关文件并说明影响范围
- 默认使用最小改动
- 不要无理由新增依赖
- 不要重写无关模块
- 不要修改现有注册流程，除非任务明确要求
- 不要修改生产环境配置
- 不要修改数据库 schema，除非先说明原因和迁移影响
- 认证、权限、支付、数据库迁移都属于高风险任务，必须先给计划并等待确认
- 改业务逻辑必须补测试
- 改 UI 必须检查 loading、empty、error、mobile 状态
- 修改后必须运行 lint、typecheck、test、build
- 如果无法运行测试，必须说明原因，并给出我应该手动运行的命令

## 登录功能规则

- 登录失败时不能泄露用户是否存在
- 密码必须使用已有 password hash 校验逻辑，不允许明文比较
- 登录成功后跳转到 dashboard
- 登录失败时展示统一错误提示
- 不允许把敏感信息写入前端
- 不允许在日志中输出密码、token、session 等敏感信息

## 完成标准

- 相关测试通过
- lint/typecheck/build 通过
- 行为符合验收标准
- diff 易于 review
- 最后必须输出：
  - 修改摘要
  - 测试结果
  - 潜在风险
  - 建议沉淀到 AGENTS.md 的规则
```

---

# 四、给 Codex 的专家级任务提示词

## 不推荐

```markdown
帮我做一个登录功能。
```

这个提示太模糊，Codex 不知道：

- 登录方式是什么
- 修改范围是什么
- 哪些文件不能动
- 验收标准是什么
- 是否需要测试
- 是否可以新增依赖
- 是否可以改注册流程

---

## 推荐

```markdown
目标：
为当前 Next.js 项目新增邮箱 + 密码登录功能。

范围：
- 前端：新增或完善登录页
- 前端：登录表单校验
- 前端：登录失败错误提示
- 后端：新增登录 API
- 后端：复用现有用户表和密码校验逻辑
- 跳转：登录成功后跳转到 /dashboard

不做：
- 不修改现有注册流程
- 不新增大型依赖
- 不修改生产环境配置
- 不做数据库迁移，除非你先说明必要性并等待确认
- 不直接提交 commit

约束：
- 保持现有 UI 风格
- 使用现有表单、按钮、错误提示组件
- 登录失败不能泄露用户是否存在
- 不在日志中输出密码或敏感信息
- 默认最小改动

验收标准：
1. 空邮箱有错误提示
2. 非法邮箱格式有错误提示
3. 空密码有错误提示
4. 错误邮箱或错误密码时，统一提示「邮箱或密码错误」
5. 登录成功后跳转到 /dashboard
6. 登录失败不泄露用户是否存在
7. 添加必要测试
8. npm run lint 通过
9. npm run typecheck 通过
10. npm test 通过
11. npm run build 通过

工作方式：
1. 先阅读相关文件，不要修改代码
2. 输出你理解的目标、影响范围、风险点
3. 给出实现计划
4. 等待我确认计划后再修改代码
5. 修改后运行测试
6. 最后输出修改摘要、测试结果和潜在风险
```

---

# 五、使用`/plan`控制复杂度

登录属于认证功能，认证是高风险模块，因此不要直接让 Codex 开始改。

## 提示词

```markdown
/plan

我要为当前项目新增邮箱 + 密码登录功能。

请先分析：

1. 你理解的目标
2. 涉及的文件
3. 需要新增的文件
4. 可能复用的现有逻辑
5. 安全风险
6. 是否需要数据库变更
7. 分阶段实现方案
8. 每阶段验收标准
9. 需要我确认的问题

要求：
- 不要修改代码
- 不要运行数据库迁移
- 不要新增依赖
```

---

## 好的计划应该类似这样

```markdown
## 1. 目标理解

为现有项目新增邮箱 + 密码登录功能。用户输入邮箱和密码后，后端验证用户身份，成功后建立会话并跳转到 /dashboard。

## 2. 涉及文件

可能涉及：

- app/login/page.tsx
- components/LoginForm.tsx
- app/api/login/route.ts
- lib/auth/password.ts
- lib/auth/session.ts
- lib/db.ts
- prisma/schema.prisma
- middleware.ts
- __tests__/login.test.tsx
- __tests__/api-login.test.ts

## 3. 风险点

- 登录失败不能泄露用户是否存在
- 不能明文比较密码
- 不能把敏感信息返回给前端
- 不能破坏注册流程
- 不能错误配置 cookie/session
- 不能绕过 middleware 权限保护

## 4. 分阶段方案

### 阶段一：确认现有认证结构

- 阅读注册流程
- 阅读 User 模型
- 阅读密码 hash 逻辑
- 阅读 dashboard 权限保护逻辑

验收标准：
- 明确可复用代码
- 明确是否需要新增 session 逻辑

### 阶段二：实现登录 API

- 新增 POST /api/login
- 校验邮箱和密码
- 使用统一错误提示
- 成功后设置 session/cookie

验收标准：
- 错误邮箱和错误密码返回相同错误
- 不返回用户敏感信息

### 阶段三：实现登录页面

- 新增登录表单
- 添加前端校验
- 成功后跳转 /dashboard
- 失败展示错误提示

验收标准：
- 空邮箱、错误邮箱、空密码都有提示
- 登录失败提示一致

### 阶段四：补测试

- 表单校验测试
- API 登录成功测试
- API 登录失败测试
- 不泄露用户存在性测试

验收标准：
- npm test 通过

### 阶段五：验证

运行：

- npm run lint
- npm run typecheck
- npm test
- npm run build
```

---

# 六、人类确认计划后，再让 Codex 实现

## 确认提示词

```markdown
计划确认。

请按你刚才的阶段方案实现。

额外要求：

1. 每次只做必要修改
2. 不要修改注册流程
3. 不要新增依赖
4. 如果发现必须修改数据库 schema，请先停止并说明原因
5. 完成后运行：
   - npm run lint
   - npm run typecheck
   - npm test
   - npm run build
6. 如果任何命令失败，请先解释原因，再修复
7. 不要删除测试
8. 不要为了通过测试而降低安全性
```

---

# 七、权限模式建议

## 1. 初始分析阶段：只读权限

适合：

- 读项目
- 找调用链
- 分析注册流程
- 审查认证逻辑
- 规划方案

建议使用：

```markdown
/permissions

切换到 read-only。
```

然后：

```markdown
请分析当前认证流程，不要修改代码。
```

---

## 2. 实现阶段：工作区可写，但需要审批

适合：

- 修改登录页面
- 新增 API
- 添加测试
- 运行测试

建议模式：

```bash
codex --sandbox workspace-write --ask-for-approval on-request
```

这样 Codex 可以在项目目录中工作，但涉及越界操作、网络访问、高风险命令时，需要你确认。

---

## 3. 高风险操作：必须禁止自动执行

登录功能涉及认证，因此这些动作必须限制：

```markdown
这是认证相关的高风险任务。

禁止执行以下操作：

1. 删除用户数据
2. 执行数据库迁移
3. 修改生产环境配置
4. 修改环境变量
5. 打印密码、token、session
6. 改动 CI/CD 或部署配置
7. 批量重构无关模块

如果你认为必须执行其中任何操作，请先停止并说明原因，等待我确认。
```

---

# 八、测试验证要求

## 给 Codex 的测试提示词

```markdown
实现完成后，请运行：

1. npm run lint
2. npm run typecheck
3. npm test
4. npm run build

如果失败：

1. 先解释失败原因
2. 再提出修复方案
3. 再进行最小修复
4. 不要删除测试
5. 不要跳过测试
6. 不要降低安全校验
7. 修复后重新运行失败的命令

如果无法运行测试，请明确说明原因，并告诉我应该手动运行哪些命令。
```

---

## 最终验收清单

```markdown
## 登录功能验收清单

### 表单校验

- [ ] 空邮箱有提示
- [ ] 错误邮箱格式有提示
- [ ] 空密码有提示
- [ ] 错误密码有统一错误提示

### 安全性

- [ ] 登录失败不泄露用户是否存在
- [ ] 不返回 passwordHash
- [ ] 不在日志中输出密码
- [ ] 使用已有 hash 校验逻辑
- [ ] session/cookie 设置合理

### 行为

- [ ] 登录成功跳转 /dashboard
- [ ] 登录失败停留在登录页
- [ ] loading 状态正常
- [ ] 移动端显示正常
- [ ] 错误提示清晰

### 测试

- [ ] API 登录成功测试
- [ ] API 登录失败测试
- [ ] 表单校验测试
- [ ] 用户存在性不泄露测试

### 命令

- [ ] npm run lint 通过
- [ ] npm run typecheck 通过
- [ ] npm test 通过
- [ ] npm run build 通过
```

---

# 九、使用`/review`做第二轮审查

代码写完后，不要马上合并。

## Review 提示词

```markdown
/review

请 review 当前未提交改动，不要修改代码。

重点检查：

1. 逻辑 bug
2. 安全风险
3. 登录失败是否泄露用户是否存在
4. 密码校验是否安全
5. session/cookie 设置是否合理
6. 边界条件
7. 测试是否充分
8. 是否修改了无关模块
9. 是否违反 AGENTS.md
10. 是否有过度设计

请按 P0 / P1 / P2 分类。

每个问题包含：

- 问题描述
- 相关文件
- 为什么有风险
- 建议修复方式
```

---

## Review 严重程度定义

```markdown
## P0

必须立刻修复的问题，例如：

- 明文比较密码
- 泄露 passwordHash
- 登录失败暴露用户是否存在
- 任意用户可绕过认证访问 dashboard
- 破坏注册流程

## P1

应该修复的问题，例如：

- 缺少关键测试
- cookie 配置不完整
- 错误处理不一致
- 边界条件遗漏

## P2

可以后续优化的问题，例如：

- 文案不统一
- 组件命名可以更清晰
- 小的可维护性问题
```

---

# 十、使用 Subagents 做多角度审查

如果这是上线前的关键功能，可以让 Codex 使用多个子代理并行审查。

## 提示词

```markdown
请使用 subagents 并行审查当前登录功能改动。

要求：
不要修改代码，只输出审查报告。

Agent A：
检查业务逻辑和边界条件。

Agent B：
检查安全、认证、权限和 session/cookie 问题。

Agent C：
检查测试覆盖是否充分。

Agent D：
检查性能和可维护性。

Agent E：
检查 UI/UX、移动端、可访问性、loading/error 状态。

最后请合并成一份 review 报告，按 P0 / P1 / P2 排序，并给出建议修复顺序。
```

---

# 十一、沉淀成自定义 Slash Command

如果你经常做认证相关改动，可以创建一个自定义命令。

## `/auth-review`

```markdown
请深度 review 当前认证相关 diff。

不要修改代码。

重点检查：

1. 密码处理是否安全
2. 登录失败是否泄露用户是否存在
3. session/cookie/token 是否安全
4. 是否有权限绕过风险
5. 是否泄露敏感信息
6. 是否破坏注册流程
7. 是否缺少关键测试
8. 是否违反 AGENTS.md

按 P0 / P1 / P2 输出。

每个问题包含：

- 问题描述
- 相关文件
- 为什么有风险
- 建议修复方式
```

以后只需要输入：

```markdown
/auth-review
```

---

# 十二、沉淀成 Skill

如果你经常做登录、注册、权限相关功能，可以封装一个 Skill。

## `auth-safe-change/SKILL.md`

```markdown
---
name: auth-safe-change
description: Use this skill when implementing or reviewing authentication, authorization, login, registration, session, cookie, token, or password-related changes.
---

When triggered:

1. Inspect existing authentication and authorization flow.
2. Do not modify code before explaining the impact scope and risk.
3. Treat authentication as high-risk.
4. Avoid changing unrelated modules.
5. Never log password, token, session, or secret values.
6. Never return passwordHash or sensitive user fields to the frontend.
7. Login failure must not reveal whether a user exists.
8. Reuse existing password hashing and verification utilities.
9. Check cookie/session/token security options.
10. Add or update tests for:
   - successful login
   - failed login
   - invalid input
   - user existence not leaked
   - protected route access
11. Run lint, typecheck, tests, and build if available.
12. Summarize:
   - changed files
   - security considerations
   - test results
   - remaining risks
```

调用方式：

```markdown
$auth-safe-change

请为当前项目实现邮箱 + 密码登录功能。
先给计划，不要马上修改代码。
```

---

# 十三、任务结束后，让 Codex 复盘并更新经验

## 复盘提示词

```markdown
请复盘这次登录功能实现过程。

输出：

1. 本次任务的原始目标
2. 实际修改了哪些文件
3. 哪些决策是关键决策
4. 哪些风险被发现或规避了
5. 哪些测试覆盖了关键行为
6. 哪些问题适合沉淀到 AGENTS.md
7. 是否应该新增或更新 Skill
8. 后续还有哪些改进建议
```

---

## 沉淀到 AGENTS.md 的提示词

```markdown
请总结这次任务中可以沉淀到 AGENTS.md 的规则。

输出格式：

1. 新增规则
2. 为什么需要
3. 建议放在哪个章节
4. 是否会影响未来任务
5. 是否可能过度约束
```

---

# 十四、完整总控提示词

你可以直接复制下面这段，用在真实项目中。

```markdown
你是我的 Codex 专家级编程搭档。

当前任务：
为当前 Next.js + TypeScript 项目新增邮箱 + 密码登录功能。

目标：
用户可以使用邮箱和密码登录，登录成功后进入 /dashboard。

范围：
- 登录页面
- 登录表单
- 登录 API
- 必要的认证 /session 逻辑
- 必要测试

不做：
- 不修改现有注册流程
- 不新增大型依赖
- 不修改生产环境配置
- 不执行数据库迁移，除非先说明必要性并等待确认
- 不改 CI/CD
- 不提交 commit

约束：
- 默认最小改动
- 保持现有 UI 风格
- 复用现有组件和工具函数
- 复用现有密码 hash 校验逻辑
- 登录失败不能泄露用户是否存在
- 不在日志中输出密码、token、session
- 不返回 passwordHash 或敏感用户字段

验收标准：
1. 空邮箱有错误提示
2. 非法邮箱格式有错误提示
3. 空密码有错误提示
4. 错误邮箱或错误密码统一提示「邮箱或密码错误」
5. 登录成功后跳转到 /dashboard
6. 登录失败不泄露用户是否存在
7. 添加必要测试
8. npm run lint 通过
9. npm run typecheck 通过
10. npm test 通过
11. npm run build 通过

工作规则：
1. 不要急着写代码
2. 先阅读相关文件
3. 先说明你理解的目标、范围、风险
4. 给出计划后等待我确认
5. 修改时保持最小改动
6. 不要无理由新增依赖
7. 修改后必须运行相关测试
8. 如果测试无法运行，说明原因和手动命令
9. 最后输出改动摘要、测试结果、潜在风险
10. 发现可复用经验时，建议写入 AGENTS.md 或 Skill

请先只读分析，不要修改任何文件。
```

---

# 十五、本实例的最佳实践总结

## 核心原则

> Codex 负责执行、探索、验证和整理；你负责目标、边界、验收和最终判断。

---

## 在这个登录功能实例中，你应该做到

| 阶段 | 最佳实践 |
|---|---|
| 开始前 | 让 Codex 只读项目，不要马上改代码 |
| 定义任务 | 明确目标、范围、不做什么、约束、验收标准 |
| 权限控制 | 初始用 read-only，实现时用 workspace-write + on-request |
| 规划 | 使用`/plan`，计划没确认前不允许改代码 |
| 实现 | 小步修改，复用现有逻辑，避免无关重构 |
| 安全 | 登录失败不泄露用户是否存在，不输出敏感信息 |
| 测试 | 要求 lint、typecheck、test、build 全部运行 |
| 审查 | 使用`/review`或 subagents 做多角度检查 |
| 沉淀 | 将反复出现的问题写入 AGENTS.md、Skill 或 Slash Command |
| 验收 | 不盲信 Codex，最终由你确认行为和风险 |

---

## 最重要的一句话

**不要把 Codex 当成 “自动写代码机器”，而要把它放进一套工程化工作流里。你给目标、边界和验收标准，它负责高强度执行。**

---

### 📋 提示词模板: 自动分类评分

[自动分类评分]

### 🤖 AI

## 自动分类评分结果

**标题**: Codex专家工作流指南

**评级**: A (72.5分)  |  **深度阅读**: 是  |  **建议操作**: 精读

**主类别**: AI编程工具
**次类别**: 软件工程工作流, 提示词工程
**标签**: #Codex #AI编程 #工程工作流 #AGENTS.md #代码审查

### 评分明细
| 维度 | 分数 | 权重 |
|------|------|------|
| 信息密度 | 7 | 10% |
| 新颖性 | 5 | 10% |
| 相关性 | 8 | 20% |
| 可操作性 | 9 | 15% |
| 方法论价值 | 8 | 15% |
| 可迁移性 | 7 | 10% |
| 证据质量 | 4 | 10% |
| 长期杠杆 | 8 | 10% |

**一句话点评**: 实操性很强的 Codex 工程化使用清单，但证据支撑和原创深度一般，部分内容有明显 AI 生成式重复感。
**核心高价值点**: 把 Codex 纳入“目标、范围、约束、验收、计划、测试、审查、沉淀”的可控工程工作流，而不是依赖单次提示词。

### 摘要
文章系统总结了专家级使用 Codex 的工程方法，包括明确目标范围、只读分析、维护 AGENTS.md、使用 /plan、/goal、权限控制、测试验证、review、subagents、Skills、MCP 和复盘沉淀。

### 知识要点
- 复杂任务应先让 Codex 只读分析项目并输出计划，再由人类确认后执行
- AGENTS.md、Skills 和自定义 Slash Command 可以沉淀团队规范和重复流程
- 高风险任务需要通过权限模式、明确禁止项和可验证停止条件控制 Codex 行为
- 代码修改后必须通过 lint、typecheck、test、build 和 review 验证

### 行动项
- 为常用项目创建或完善 AGENTS.md，写入构建、测试、Review 和依赖规则
- 整理个人常用的 Codex 任务模板，如读项目、修 bug、重构、补测试和 review
- 在复杂开发任务中采用先 /plan、确认后小步实现、完成后 /review 的流程
- 将高频工作流封装为 Skill 或自定义 Slash Command

### 存档判定
归档: 是 — 原因: 包含较完整的 Codex 使用流程、权限控制、规划、测试、审查和经验沉淀方法，具备较强实操价值
删除: 否
留存决策: 长期保留，可作为使用 Codex 进行工程开发的工作流参考

---

