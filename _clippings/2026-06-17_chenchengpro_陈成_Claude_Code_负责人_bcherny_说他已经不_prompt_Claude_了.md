---
title: "2026-06-17_chenchengpro_陈成_Claude_Code_负责人_bcherny_说他已经不_prompt_Claude_了"
source: "https://x.com/chenchengpro/status/2064221035734646916"
author:
  - "[[@chenchengpro]]"
published: 2026-06-17
created: 2026-06-17
description:
tags:
  - "x"
  - "@chenchengpro"
  - "agent"
  - "💬"
---

# 陈成: Claude Code 负责人 @bcherny 说他已经不 prompt Claude 了

**陈成**

Claude Code 负责人 [@bcherny](/bcherny)

说他已经不 prompt Claude 了。是循环在 prompt Claude、在决定下一步，他的活变成了写那个循环。Addy Osmani 给这事起了个名，叫 loop engineering：你不再是那个一轮轮敲 prompt 的人，而是搭一个会自己找活、分发、检查、记账、定下一步的系统，让它去捅 agent。

循环就五个零件，Claude Code 和 Codex 现在都备齐了。Automations 是心跳：定时自己跑、自己发现和分流，有结果进 Triage 收件箱，没结果就归档（/loop 按周期重跑，/goal 跑到你写的可验证条件为真才停，每轮完一个独立小模型来判断算不算完成，写代码的 agent 没资格给自己打分）。Worktrees：git worktree 给每个并行 agent 一份独立 checkout，从机制上断了它们踩同一个文件的可能。Skills：SKILL.md 把项目知识摊在外面，省得 agent 每次像金鱼一样重猜一遍，没它循环每轮都从零重推你的项目。Connectors（建在 MCP 上）：让循环够得着 issue tracker、数据库、staging API、Slack，差别就在「这是修复方案」和「自己开 PR、关联 ticket、CI 绿了就 ping 频道」之间。Sub-agents：写的人和检查的人分开，毕竟写代码的模型给自己打分总是手软。

还有第六件，最不起眼，可能也最要命：memory。一个 markdown 文件，或者一块 Linear board，活在单次对话之外，记着做过什么、下一步干嘛。模型每跑一次就忘光上一次，所以记忆得落在磁盘上，不能搁在 context 里。一句话，agent 会忘，repo 不会。

但有三件事循环没替你解决，而且循环越顺，这三件越扎手。第一，验证还是你的事：没人盯着的循环，也是在没人盯着地犯错，done 只是它的声明，不是证明。第二，你的理解会烂掉：循环越快地 ship 那些你没亲手写的代码，你和代码之间的鸿沟就越宽。第三，最舒服的姿势往往最危险——循环自己转起来，你很容易就不再有观点、照单全收。所以那句话是对的：Build the loop, stay the engineer，循环你来搭，但工程师这个位子得你自己坐着。

> **@addyosmani**
> 
> Addy Osmani @addyosmani · 10h 文章 循环工程 循环工程正在取代你作为提示代理的角色。你设计一个系统来替代你完成这个任务。这里的循环可以被视为一个递归目标，在这个目标中你定义一个目的... Addy Osmani @addyosmani · 10h 文章 循环工程 循环工程正在取代你作为提示代理的角色。你设计一个系统来替代你完成这个任务。这里的循环可以被视为一个递归目标，在这个目标中你定义一个目的...

![引用图片](https://pbs.twimg.com/media/HKU_Us-bMAAZO3J?format=jpg&name=large)

* * *

### 热门回复

**@Viking** ♥ 494 · 💬 22

分享一篇文章：《How LLMs Actually Work》

https://

0xkato.xyz/how-llms-actua

lly-work/

…

好像是前几天 HackerNews 排名第一来着，类似的文章很多，但是这篇深入浅出和直观的例子非常适合有一定编程但没深入学Transformer的人阅读，里面的比喻也恰当，一看就是活人写的，没什么 AI 味道。

**@宝玉** ♥ 237 · 💬 164

微信格局还是不够，总是想着大家都去他们家一亩三分地耕耘，还幻想着未来微信会继续是超级入口，人人都在用微信，所以只需要让 AI 去操作小程序。

但现实是，未来微信的入口属性会越来越少，以后的年轻人，不会再去打开微信，只会问自己的

**@陈成** ♥ 345 · 💬 43

Cloudflare CTO 随口问了句"大家都怎么用 loops?"，三分钟后自己补刀"To orchestrate agents"，结果 245 条回复把 2026 年中 AI agent 圈最热的"循环"玩法扒了个底朝天。

真正有料的就四类。CI/PR 保姆：循环盯 CI 跑绿、监控 PR 评论、改完自动同步 README 和下游文档，有人让 agent

**@Max For AI** ♥ 118 · 💬 28

没想到微信官方这个Skill最佳实践文档居然写得这么好？？

里面几个点写得特别细。

比如用户意图模糊时，什么时候该追问，什么时候该直接给默认方案，什么时候该把多个可能意图列出来让用户选。

**@Benjamin Meng** ♥ 0 · 💬 0

有几个文件现在完全是agent写的，我改都不敢随便改，怕动一行崩一片。速度是快了，但"我的项目里有些地方我自己看不懂"这感觉挺别扭的。

---

<br>

## 💬 对话记录

### 📋 提示词模板: 解读

[Prompt: 解读]

### 🤖 AI

# 深度总结

这篇内容讲的是：**AI 编程正在从“写 prompt”进入“搭循环系统”阶段。**

过去，人负责一轮轮提示 AI：  
> 写代码 → 看结果 → 再提示 → 再修改。

现在，Claude Code / Codex 这类工具开始支持一种新模式：  
> 人不再手动 prompt，而是设计一个自动循环，让 AI 自己发现任务、执行、检查、记录、推进下一步。

Addy Osmani 把它叫做 **Loop Engineering，循环工程**。

核心观点一句话：

> 未来厉害的不是会写 prompt 的人，而是会搭 AI 工作循环的人。

---

# 什么是 Loop Engineering？

简单说，就是把 AI 变成一个“自动干活系统”。

它不只是回答问题，而是可以：

1. 定时启动；
2. 找到要做的任务；
3. 分配给不同 agent；
4. 写代码；
5. 自测或交给另一个 agent 检查；
6. 记录进度；
7. 决定下一步；
8. 必要时开 PR、关联 ticket、通知团队。

人从“操作员”变成“系统设计者”。

---

# 循环系统的 6 个关键零件

## 1. Automations：心跳

让 agent 定时跑起来。

例如：

- 每天检查 issue；
- CI 挂了自动修；
- PR 有评论自动处理；
- 任务没完成就继续跑。

它解决的是：**AI 不再需要人每次手动叫醒。**

---

## 2. Worktrees：隔离工作区

用 `git worktree` 给不同 agent 分配独立代码副本。

好处是：

- 多个 agent 可以并行工作；
- 彼此不会互相覆盖文件；
- 出错也更容易回滚。

它解决的是：**多个 AI 同时干活时互相踩踏的问题。**

---

## 3. Skills：项目知识

通过 `SKILL.md` 这类文件，把项目规则、架构、约定写清楚。

否则 agent 每次都像失忆一样重新猜：

- 项目怎么启动？
- 代码风格是什么？
- 哪些目录不能动？
- 测试怎么跑？

它解决的是：**AI 每轮都从零理解项目的问题。**

---

## 4. Connectors：外部连接

基于 MCP 等机制，让 agent 能连接：

- GitHub / GitLab；
- Linear / Jira；
- 数据库；
- Slack；
- Staging API；
- CI 系统。

这会带来巨大差异：

普通 AI 只能说：  
> 这是修复方案。

接入系统后的 AI 可以：  
> 我已修复、开 PR、关联 ticket、CI 通过，并通知了频道。

它解决的是：**AI 从“建议者”变成“执行者”。**

---

## 5. Sub-agents：分工与互审

写代码的 agent 和检查代码的 agent 要分开。

因为：

> 写代码的模型给自己打分，往往会手软。

所以需要：

- 一个 agent 写；
- 一个 agent 审；
- 甚至另一个小模型判断是否达成目标。

它解决的是：**自我验证不可靠的问题。**

---

## 6. Memory：外部记忆

这是最不起眼但最关键的一点。

模型本身会忘。  
所以记忆不能只放在上下文里，而要落到外部：

- Markdown 文件；
- issue board；
- Linear ticket；
- repo 文档；
- changelog。

核心句：

> Agent 会忘，repo 不会。

它解决的是：**AI 每次运行都断片的问题。**

---

# 深入浅出地理解

可以把 Loop Engineering 想成：

## 以前：你在指挥一个实习生

你每次都要说：

> 你去看这个文件。  
> 改这里。  
> 跑测试。  
> 报错了再修。  
> 现在提交 PR。

你很累，因为每一步都要盯。

---

## 现在：你在设计一条生产线

你定义好：

- 什么时候启动；
- 从哪里拿任务；
- 谁负责写代码；
- 谁负责检查；
- 怎么判断完成；
- 怎么记录进度；
- 出错怎么办；
- 成功后通知谁。

然后系统自己转。

这就是从 **prompting** 到 **loop engineering** 的变化。

---

# 最重要的警告

文章最后强调：循环系统很强，但它没有替你解决三个核心问题。

## 1. 验证仍然是你的责任

AI 说 done，不代表真的 done。

尤其是无人看管的循环，可能只是：

> 自动化地犯错。

所以最终还是需要人建立可靠验证：

- 测试；
- 代码审查；
- 指标监控；
- 回滚机制；
- 安全边界。

---

## 2. 你的理解可能会腐烂

AI 写得越快，你越容易失去对代码的掌控。

最后会出现一种尴尬：

> 项目是我的，但里面很多代码我看不懂。

短期效率变高，长期可能变成技术债。

---

## 3. 你可能失去判断力

最危险的是：系统太顺了，人开始照单全收。

AI 开 PR，你合并；  
AI 说修好了，你相信；  
AI 说测试通过，你放心。

久而久之，工程师从决策者变成审批机器。

---

# 辩证思考

## Loop Engineering 是进步

它确实代表软件开发的新阶段。

因为它把 AI 从“聊天工具”推进到“工作流执行系统”。

真正的价值不再是单次回答，而是持续完成任务：

- 自动修 bug；
- 自动补测试；
- 自动更新文档；
- 自动处理 PR 评论；
- 自动维护项目状态。

这会大幅提高工程效率。

---

## 但它不是工程师的替代品

循环能执行，但不天然理解业务目标。  
循环能产出代码，但不天然保证正确。  
循环能自检，但不天然具备责任感。

所以人的角色没有消失，只是变化了：

> 从写每一行代码，变成设计系统、设定边界、定义标准、承担判断。

---

## 未来更重要的能力

不是“会不会问 AI”，而是：

1. 能不能把目标拆成可验证任务；
2. 能不能设计可靠循环；
3. 能不能让 agent 有记忆、有工具、有边界；
4. 能不能识别 AI 产出的错误；
5. 能不能保持对系统的理解。

也就是说，工程师会更像：

> 架构师 + 流程设计师 + 审计员 + 最终责任人。

---

# 一句话结论

**不要只学怎么 prompt AI，要学怎么搭 AI 循环。**

但更重要的是：

> Build the loop, stay the engineer.  
> 循环可以自动转，但工程判断不能外包。

---

### 📋 提示词模板: 自动分类评分

[自动分类评分]

### 🤖 AI

## 自动分类评分结果

**标题**: Loop工程的六个组件

**评级**: A (75.5分)  |  **深度阅读**: 可选  |  **建议操作**: 精读

**主类别**: AI工程
**次类别**: Agent系统设计, 软件工程方法论
**标签**: #Loop Engineering #Claude Code #AI Agent #自动化工作流 #软件开发

### 评分明细
| 维度 | 分数 | 权重 |
|------|------|------|
| 信息密度 | 8 | 10% |
| 新颖性 | 7 | 10% |
| 相关性 | 8 | 20% |
| 可操作性 | 7 | 15% |
| 方法论价值 | 8 | 15% |
| 可迁移性 | 8 | 10% |
| 证据质量 | 4 | 10% |
| 长期杠杆 | 8 | 10% |

**一句话点评**: 一篇高密度的Agent工作流方法论短文，框架清晰，但证据和案例支撑较弱。
**核心高价值点**: 将Agent使用从“写prompt”提升为“设计可验证、可记忆、可分工的执行循环”。

### 摘要
文章介绍了从手动prompt转向构建自主循环系统的Loop Engineering理念，概括了Automations、Worktrees、Skills、Connectors、Sub-agents和Memory六个关键组件，并提醒验证、代码理解和工程师判断仍不可外包。

### 知识要点
- Loop Engineering强调构建能自动发现任务、执行、检查、记账并决定下一步的系统，而不是人工逐轮prompt。
- Automations、Worktrees、Skills、Connectors、Sub-agents和Memory构成Agent循环的核心基础设施。
- 写代码的Agent不应自行判断任务完成，需要独立检查者或验证机制。
- Memory应落在磁盘、repo或任务系统中，而不是仅存在于上下文窗口里。
- 循环越自动化，越需要人类工程师保持验证能力、代码理解和独立判断。

### 行动项
- 为自己的AI开发流程设计一个最小Loop：任务输入、执行、验证、记录和下一步决策。
- 在项目中尝试使用git worktree隔离多个Agent并行修改。
- 为项目补充SKILL.md或类似知识文件，减少Agent重复推理项目背景。
- 建立独立验证环节，避免让生成代码的Agent自己判定完成。
- 将Agent执行历史和下一步计划写入持久化文件或任务系统。

### 存档判定
归档: 是 — 原因: 文章提炼了AI Agent循环工程的关键组件和风险边界，可作为设计自动化开发工作流的参考框架。
删除: 否
留存决策: 长期保留

---

### 📋 提示词模板: 结构化

[Prompt: 结构化]

### 🤖 AI

## 主旨概括

文章认为，AI 编程正在从“人不断写 prompt 指挥 agent”转向“人设计自动循环系统，让 agent 自己找任务、执行、检查并推进下一步”。这种方式被称为 **Loop Engineering（循环工程）**，但作者提醒：循环可以自动化很多流程，工程师仍必须负责验证、理解和判断。

---

## 1. 从 Prompt 到 Loop：人的角色变了

Claude Code 负责人表示，他现在已经不再一轮轮地 prompt Claude，而是让一个“循环”去 prompt Claude、判断下一步该做什么。

Addy Osmani 将这种模式称为 **Loop Engineering**：

- 过去：人不断输入 prompt，指导 AI 做每一步。
- 现在：人搭建一个系统，让它自动：
  - 找任务
  - 分发任务
  - 检查结果
  - 记录进度
  - 决定下一步
  - 调用 agent 执行

换句话说，人的工作从“亲自指挥 AI”变成了“设计一个会自动指挥 AI 的循环系统”。

---

## 2. 循环工程的五个核心零件

作者认为，Claude Code 和 Codex 现在已经具备了循环工程所需的五个关键组件。

### 2.1 Automations：循环的“心跳”

Automations 负责让系统定时自动运行，并处理任务流转。

关键能力包括：

- 定时启动任务
- 自动发现问题
- 自动分流结果
- 有结果就进入 Triage 收件箱
- 没结果就归档

文中提到两个例子：

- `/loop`：按周期反复运行。
- `/goal`：一直运行到某个可验证条件成立才停止。

重要的是，每一轮完成后，会由一个独立的小模型判断任务是否真的完成。写代码的 agent 不能给自己打分，因为它可能“手软”。

---

### 2.2 Worktrees：避免多个 agent 互相踩代码

通过 `git worktree`，可以给每个并行运行的 agent 分配一份独立的代码 checkout。

这样做的好处是：

- 每个 agent 在自己的工作区修改代码。
- 不会多个 agent 同时改同一份文件。
- 从机制上减少冲突和混乱。

---

### 2.3 Skills：让 agent 不再每次从零理解项目

`SKILL.md` 用来保存项目知识，相当于把项目背景、规则、约定写在外部文件里。

它的作用是：

- 让 agent 每次运行时能快速理解项目。
- 避免像“金鱼”一样每轮都重新猜项目结构。
- 降低重复推理成本。

如果没有 Skills，循环每一轮都可能从零开始重新理解项目。

---

### 2.4 Connectors：让循环连接真实工作系统

Connectors 建在 MCP 之上，让循环能够接入外部系统，例如：

- issue tracker
- 数据库
- staging API
- Slack

这会带来很大差别：

- 没有 Connectors：agent 只能说“这是修复方案”。
- 有 Connectors：agent 可以自己开 PR、关联 ticket、跑 CI，CI 绿了之后再通知 Slack 频道。

也就是说，agent 不只是给建议，而是能进入真实研发流程。

---

### 2.5 Sub-agents：写代码和检查代码要分开

作者强调，写代码的 agent 和检查代码的 agent 应该分离。

原因很简单：

- 写代码的模型给自己打分往往不够严格。
- 独立检查可以减少“自我确认”的问题。
- 更接近真实工程里的开发与 code review 分工。

---

## 3. 第六个关键组件：Memory

除了五个核心零件，作者认为还有一个不起眼但极其重要的东西：**memory（记忆）**。

这个记忆可以是：

- 一个 markdown 文件
- 一块 Linear board
- 其他持久化记录系统

它需要记录：

- 已经做过什么
- 当前进展如何
- 下一步要做什么

原因是：模型每次运行后都会忘记上一次的状态，所以记忆不能只放在 context 里，而要落到磁盘或外部系统中。

作者用一句话总结：

> agent 会忘，repo 不会。

---

## 4. 循环工程无法替你解决的三个问题

虽然循环可以让 agent 更自动化、更高效，但作者提醒，有三件事仍然必须由工程师负责。

### 4.1 验证仍然是你的责任

没人盯着的循环，也可能在没人盯着的时候犯错。

“done”只是 agent 的声明，不代表任务真的完成。

所以工程师仍然要负责：

- 验证结果
- 检查代码质量
- 确认业务逻辑正确
- 确认没有隐藏问题

---

### 4.2 你对代码的理解可能会变差

循环越快地交付那些你没有亲手写的代码，你和代码之间的距离就越远。

这会带来一个问题：

- 项目跑得更快了。
- 但你可能越来越看不懂自己的项目。

热门回复里也有人提到类似感受：

> 有几个文件现在完全是 agent 写的，我改都不敢随便改，怕动一行崩一片。速度是快了，但“我的项目里有些地方我自己看不懂”这感觉挺别扭的。

---

### 4.3 最舒服的姿势往往最危险

当循环自己顺畅运转后，人很容易变成“照单全收”。

风险在于：

- 不再主动判断
- 不再保持技术观点
- 不再质疑 agent 的输出
- 逐渐把工程责任交给系统

但作者认为，工程师不能放弃自己的位置。

---

## 5. 最后的核心提醒

作者用一句话总结循环工程的正确态度：

> Build the loop, stay the engineer.

也就是说：

- 循环可以由你来搭建；
- 自动化可以帮你推进很多工作；
- 但工程判断、验证责任和系统理解，仍然必须由你承担。

---

