---
title: 2025-11-14_LangChain实战：打造具备记忆、HIL、工具调用的ReAct Agent，并提供API服务
source: ""
author:
  - ""
published: 2025-11-14
created: 2025-11-14
description: ""
tags:
---

# LangChain实战：打造具备记忆、HIL、工具调用的ReAct Agent，并提供API服务

YouTube 视频地址：https://www.youtube.com/watch?v=kR2tJk5lVwE

## 1、项目介绍

### 1.1 主要内容

本期视频主要分享的内容：

*   使用FastAPI框架实现对外提供Agent智能体API后端接口服务
*   使用LangGraph中预置的ReAct架构的Agent
*   支持Short-term短期记忆并使用PostgreSQL进行持久化存储
*   支持Function Calling，包含自定义工具和MCP Server提供的工具
*   支持Human in the loop(HIL)人工审核对工具调用提供人工审查功能，支持四种审查类型
*   支持多厂商大模型接口调用，OpenAI、阿里通义千问、本地开源模型Ollama等
*   支持Redis用户会话平台，支持客户端的故障恢复和服务器端的故障恢复
*   使用功能强大的rich库实现前端Demo应用，与后端API接口服务联调

本期视频是在ReActAgentsTest项目的基础上进行更新，所使用的代码文件夹为04_ReActAgentHL ApiTest, 源码及相关资料均免费开源共享给大家，大家可以进入到下面给出的项目，在项目中下载对应的文件夹即可。

关于如何构建ReAct agents test的项目，大家参考本期系列视频的第一集视频，有详细的实操演示，链接地址我放到这个地方，大家可以详细去看一下。

### 1.2 后端业务核心流程

关于代码中涉及到的前后端业务核心流程，大家在我提供的文件夹里面，在Doc这个文件夹下面有三个文件，那三个文件对应的就是：

*   后端业务核心流程
*   API接口和数据模型的描述
*   前端业务核心流程

那里面把代码相关的业务逻辑都给大家使用示意图或流程图的方式给大家画出来了。那我们在后面给大家实操演示完成之后，再回头给大家读一下这三个文件，也就是给大家梳理一下前后端代码的逻辑。

关于本期视频所涉及到的技术框架，这边列举了两个比较核心的技术框架，一个是LangGraph，一个是MCP。那关于这两个框架我都有很多期视频去分享，那大家可以去参考我这边所列举出的相关的项目和视频，大家可以做详细的了解，这边就不给大家详细去赘述了。

### 2、前期准备工作

那下面我们来看一下前期准备工作。前期准备工作呢，主要涉及了两块，一个是开发环境的搭建，一个是大型服务接口的调用，那这块呢，大家可以去参考刚刚在上面给大家所列举的，在构建这个项目的那期视频里面有关于它们的详细的介绍。

### 3、项目初始化

接下来是项目的初始化，那本期视频呢，我们是在这个项目的基础上进行的，所以只需要将这个文件夹源码下载下来，放到它项目的根目录下面就可以了。那最后就是安装项目的依赖。那本期视频会多几个项目的依赖包，那大家按照我这个顺序去安装就可以了。那这边的话就不给大家具体去安装了，大家只要把这几个给跑一下就可以了。

### 4、功能测试

好，下面最后我们来看一下功能测试。还是一样，先给大家演示整个的案例的整个的一个全流程的演示，看一下现象之后再具体分析一下前后端代码的逻辑。

那关于功能测试，首先第一块是使用Docker的方式运行PostgreSQL数据库和Redis数据库。那大家关于Docker的安装，可以进入到官网去下载对应的软件，安装完成之后打开软件。进入我这边提供给大家的一个文件夹，里面把两个相关的Docker compose的文件已经给到大家了，那大家只要去执行这个指令去安装就可以了。那下面的话我们就来为大家实操演示一下。

运行Docker软件之后，大家可以看到这样的一个页面，那目前的话里面的容器和镜像都是空的。接下来我们打开一个命令行终端，进入到Docker文件所在的文件夹，也就是这个地方。我们进入到当前的文件。

那进入到之后呢，我们首先来启动一下PostgreSQL的服务。那我们只要去复制这条指令，并且我们进入到当前的这个文件夹去执行。那这边的话大家可以看到它首先会检查你本地的仓库里面有没有PostgreSQL的镜像文件，如果说没有的话，它会去到相应的网址进行拉取，拉取之后再进行后台的启动。那这边的话我们就等待它完成之后再继续。

那完成之后，同样地步骤再来启动一下Redis服务。那到这里呢，PostgreSQL数据库和Redis数据库的服务就已经启动完成。完成之后可以再回到这个软件里面来，大家看一下。那大家在这边的容器里面就可以看到，当前两个服务已经在正常的运行中，那对应的本地仓库的镜像也有了这两个服务的镜像文件。

那接下来我们可以通过远程客户端的方式，那对于PostgreSQL大家可以使用这个客户端，那对于Redis，客户端呢，大家可以使用这个客户端软件。下面的话给大家看一下，那打开客户端软件之后，大家只要去进行相关的连接，那连接的话，大家只要根据你所开启的PostgreSQL的服务，那大家如果说是根据我的操作来的，大家只要按照这样去配置就可以了。那配置完成之后大家可以去测试，测试完成之后，我们双击打开，打开之后大家可以看到，这边的话会多了一个数据库，我们可以刷新一下。那目前的话这个数据库里面的表是空的。好，那这个是PostgreSQL的，通过客户端软件远程连接到当前的数据库服务，可以很方便的去查看和管理。那对于Redis呢，大家可以使用这个软件，那也是一样的，大家只要在你的本地进行一个连接，那连接之后大家可以进入到这样一个页面，那当前也是一个空的，没有内容。

那接下来我们再回到我们的操作手册里面来。那所需的两个服务启动完成之后，下面就进行功能测试。功能测试主要涉及到两份代码，第一份是后端的服务，那对应的代码在这个地方，对应的是它。那关于前端的服务对应的是它。那如何去测试呢？大家只要去运行两个服务就可以了。那在运行之前，大家要首先去设置一下你的大模型的参数，那大模型的参数大家可以在运行之前，首先大家要进入到当前的这个文件夹，那这个文件夹下面呢，提供了三个脚本，第一个脚本是全局的一个配置的脚本。那在这个脚本里面呢，主要是涉及到对于log日志的参数的配置，以及PostgreSQL数据库配置的一个参数。那这边你要根据自己的一个实际的需求进行相关的调整。再一个是Redis数据库的配置参数的调整，以及使用哪一个大模型厂商的大模型。那这边的话，我先使用代理的方式使用OpenAI的模型。

那紧接着是一个我们的后端服务API服务的地址和端口。那如果说你是跟着我的步骤来的，那这边的话你基本上不用去做任何的调整。那第二个脚本呢是关于大模型的配置，那这边提供了一个统一的大模型配置的脚本。在这个脚本里面，大家只要在这一块去做相应的配置即可。那这边主要是提供OpenAI，也就是我们使用代理的方式。那通过代理的方式的话，大家可以去在这个代理平台申请API key以及你所使用的哪一个模型。这边给大家测试使用的是gpt-4o-mini模型，或者你也可以使用Deepseek这个模型。那紧接着是API，那这个就不给大家啰嗦了。再一个就是千问，那千问的话主要是要使用阿里的百炼平台上面的模型。这边的话使用的是qwen-turbo-latest，也是qwen-3对应的模型，所以本期视频的话也会为大家去测试一个OpenAI的模型，一个千问的模型。那对于Ollama的模型，大家也可以去尝试使用。那在模型的选择上面来说的话，大家一定要注意，你的模型一定要能够支持Function calling。那对于Ollama本地开的大模型呢，大家可以使用Llama3.18B这个模型进行测试。

好，这是大模型的测试。那这边的话，要根据你自己的大模型配置参数进行调整。那最后一个呢，是提供了对于工具，那这边的话我们是自己实现了两个自定义工具，以及使用MCB server的工具，也就是使用的是高德地图的MCB server。那这个我们在后续的实操演示环节，再给大家详细去讲解。那这个是三个脚本，那对于我们的主脚本来说的话，大家在运行这个脚本之前一定要去做相应的配置。

配置完成之后呢，我们就来运行这个两个脚本。那这边的话，大家一定要进入到当前脚本所在的文件夹。首先我们来运行第一个脚本，也就是我们启动我们的后端服务。那后端服务运行成功之后，大家可以先去数据库里面去刷新一下这个数据库，那这个时候大家可以看到它已经创建了四个表，那这个四个表呢，其实是针对于LangGraph中关于短期记忆，也就是基于线程级的记忆，他所给大家实例化出来的四个关于checkpoint存储的表。那这个呢在我之前分享的视频里也给大家介绍过。

好，那接下来再来去打开一个命令行终端，来运行一下前端的服务。那也是一样进入到当前文件脚本所在的文件夹。运行成功之后先不着急去进行测试，先来看一下代码结构。那在代码结构的话，大家可以看到多了一个文件夹，关于日志的存储的文件夹，那打开之后大家可以看到，关于后端服务所有的日志的记录都在这个文件里面。那再一个呢，就是关于我们后端服务的一个启动的日志，那我们当前跑在的端口是这个端口，所以在我们启动前端服务的时候，大家一定要在你的前端配置后端服务API的URL地址的时候，一定要记得跟你这边的IP和端口进行匹配。

接下来就给大家去做测试。那做测试的话，首先我们先来去测关于对工具请求进行人类反馈的四种反馈类型。那这边的话，在代码中是使用了Python实现了两个工具，但这两个工具都是模拟的工具，比较简单。第一个是模拟的酒店预订的工具，那在我本期系列的视频前几期也都为大家举了这样的一个工具的例子，那大家如果说看过我前几期视频呢应该不会陌生。还有一个就是实现了一个计算两个数乘积的工具，所以这两个工具比较简单。那我们通过这两个简单的工具，我们来给大家测试一下人类反馈。那具体的测试流程，首先我们来给大家测试一下输入“yes”，也就是接受工具的调用，我们来执行一下。那这边的话，大家可以直接点击Enter键直接进入，那这边的话整个的一个交互的逻辑就是，这边需要输入用户的ID。那这边的话你可以自己可以去定义你的用户ID，你也可以不填写，那不填写的话就是默认使用的是这个。所以这边的话我把它用户ID设置为test01，那这个时候我点击回车。好，这个时候他在我的服务端没有找到我当前用户所对应的会话，因为我是一个新的用户，所以他在我的服务端会创建一个新的会话。那这个时候他就会让我输入我的问题，那这边有几个特殊的指令，我们再后面再给大家去测。那下面的话我们就先来去问一个问题，就是调用工具预订酒店，我们点击回车。那这个时候我们可以等待一下他大模型的一个回复。那这个时候大家可以看到，大模型所给我的回复是，这个时候产生了一个工具的中断。那中断的话就是需要准备去调用这个工具，也就是我这边实现了这个工具，并且当前的参数是如家酒店，因为在我的问题里面，我是要他预订如家酒店，所以他把我的参数匹配上。那匹配上之后，他首先他要去调用这个工具之前，因为我们对这个工具进行了人类反馈的一个设置，所以他需要我前端去进行相关的反馈。那这个时候有几种选择，那四种这个反馈的类型。第一个是接受工具的调用，第二个是拒绝工具的调用，第三个是修改工具参数后调用工具，第四个是不调用工具，直接反馈信息。我们就来一一测试一下。那首先这边我们先用同意工具调用来试一下。好，那这边的话我们就可以看一下。那这个时候他已经成功预订了在如家酒店的住宿。那这个是智能体的最终的一个回复。

那为了方便大家去理解，我在我的服务端把相关的上下文内容里面都打印出来了，大家可以去看一下它的这个过程。那我们这边的话可以，这个是最终大模型的回复的一个内容。那我们再往前看一条，那这边展示的是工具的回复的内容。那工具的回复内容就是成功预订了在如家酒店的住宿。所以这个时候大家可以看到，他其实大模型最终给我回复的时候，他是参考了工具的调用。那再往前走一个，就是他在工具调用的时候，所匹配的参数。那关于前端服务的代码呢，我们也可以来看一下，那大家对于前端的一个代码结构，大家只要去通过一步一步地去追就可以了。比如说它的一个main脚本是从这里开始，那大家就要从这个函数开始看，那每一行去分析里面具体的一个逻辑。那其他的都是基于你整个的一个业务流程所衍生出来的一些方法，封装的单独封装的一些方法。那这个我会列了一个流程图，会给大家详细去介绍，所以这里面的代码结构就是这样代码结构。那关于这个两个具体的，核心的逻辑，我们通过提供给大家的三个文件来给大家去介绍，大家通过这个三个文件的内容的介绍，大家再反过来自己去读相关的代码，那大家就很容易去理解了。

首先关于后端这个脚本，那前面这一块的话，我们是使用了一个一个第三方的库，我们使用它去做相关日志的记录，那这边也没有什么，主要是去设置一些你的日志文件，最大允许的一个大小，以及达到上限之后的一个触发的一个轮转。那这块的话，大家可以详细去了解一下。那紧接着我们整个的后端服务主要是来通过FastAPI框架去实现接口，那这边主要实现了5个接口。那5个接口的话，我们这个代码应该如何去看呢？首先我们会使用FastAPI去实例化一个APP，那通过这个APP，我们去设置了一个生命周期函数。那在你的服务一启动的时候，会立马去初始化这个生命周期函数。那在这个生命周期函数里面，去进行相应的初始化。那最终我们通过编写POST的接口和GET接口去实现相关的一个功能。

那对于前面这几个呢，主要都是为了去实现这个接口所提供的一些额外的，一个方法，那主要是涉及到，一个Redis的存储，那Redis呢主要是用来对整个的会话状态进行管理。那再一个就是解析一个state消息列表进行格式化展示，也就是大家在这个服务端所看到的这样的一个，一个比较友好的一个可视化的一个展示，那这块的话在你代码实际运行过程中，你可以把它做死掉的，就是不需要输出这些东西。

那这个就是整个的一个代码逻辑。所以这个结构就是这样的一个结构。那关于前端的服务的脚本也是一样，给大家看一下整个的一个一个结构。那关于这个两个的具体的核心的逻辑，我们通过提供给大家的三个文件来给大家去介绍。大家通过这个三个文件的内容的介绍，大家再反过来自己去读相关的代码，那大家就很容易去理解了。

首先来给大家介绍一下这个后端业务核心流程，我们先把它打开。关于后端服务，我给大家提供了两个文件，一个是核心的流程，再一个就是API接口和数据模型的描述。那首先我们来看一下第一个文件，那第一个文件呢，主要想给大家介绍的是关于这个LangGraph中的一个HIL，也就是人工审查这样的一个功能。那想要去实现带有HIL功能的Agent系统，其关键在于流程的中断和恢复，以及为了支持它所需要的状态持久化机制。简单来说，就是需要一种机制将流程挂起在特定节点，等待人类参与和反馈，然后能从中断点恢复运行。那LangGraph提供的解决方案是中断加上命令恢复，再加上检查点三大机制。

那关于中断就是暂停LangGraph工作流的执行的同时，返回一个中断数据对象，其中包含给人类的信息，比如需要审核的内容。那第二个是命令的恢复，要恢复工作流，要获得人类反馈信息，并注入工作流状态，也就是上下文，然后发出继续执行的命令。那执行命令的话，在LangGraph中它是使用这个command加上一个resume这样的一个参数来反馈并且恢复。那checkpoint的话就是为了实现断点续跑，必须要实现Agent的状态持久化存储，用来在做恢复时重建现场。那整个的一个流程，其实就是大概这样的一个流程。在节点运行到一个人工审查这样的一个节点的时候，它先去挂起，挂起之后等待用户的反馈，最后去恢复。那最终紧接着再去执行下一个节点的执行。

那我们有了这样的一个概念之后，我们其实再来看一下，我们的这个代码里面的一个逻辑，那这个逻辑呢，就是为大家拆解了一次中断处理过程的一个流程的描述。那首先我们可以看一下，那这个是我们的用户，那用户使用的是一个用户客户端软件，对吧？那这个客户端，那关于这个客户端呢，它会去调用一个invoke方法，那这个invoke就是LangGraph中它所提供的关于图运行的一个方法。那使用这个方法，我们把相关的输入和thread ID。那这个thread ID就是来区分你的每一次checkpoint的一个唯一的ID。那关于这个方法运行之后，它就会工作流就会去运行，也就是在这个graph这个工作流里面就会去运行。那在节点的运行的过程中，比如说到了这个地方，有一个人工审查这样一个节点，触发了中断。那触发了中断之后，他会把中断数据携带者给到这个客户端软件。那这个时候客户端软件可能会提示用户，在这个客户端软件的页面上，告诉用户，你需要去进行相关的一个反馈。那反馈的那个就是这边三个，也就是这个中断发生之后，客户端收到Agent的返回状态，从中发现有中断，则提示用户进行反馈。那用户把反馈信息再给到输入到你的客户端，那客户端去调用invoke恢复工作流。那指定对应的现场ID和你的resume相关的一个信息去恢复现场。那最后呢，也是在这个地方。那会话完成之后，那第十步就是一次中断过程处理结束，会话结束。

所以整个的一个业务端，后端的一个业务端的整体的一个核心的流程框架就是这样子的。那其实就是来写相关的接口，并且按照这样的一个核心的流程来去进行相关的一个处理和状态和这个会话状态的记录。那了解了这些核心的流程之后呢，我们再来看一下后端到底做了什么。那后端的话我们可以把它放大来看一下。

首先关于API接口的描述，这一共是提供了5个API接口，那每个API接口它对应的请求的类型，以及相关的描述，那这边也都给大家列出来了，大家也可以详细去看一下。那关于它的请求参数以及返回内容，那这边也有相关的介绍。那这里面会涉及到一些数据对象，那这个数据对象呢，我们是会去提前去定义这个数据模型。所以这些数据模型的话，大家可以去看一下，那关于第一个数据模型，就是客户端发起的智能体请求。那这个数据模型主要是来约束你的前后端大家如何去使用相关的字段，要提前做好约定，这样的话你的前后端的交互怎么去进行联调，就是比较规范，那这样做的好处是什么呢？好处是，你前后端其实是可以同时开发的。因为你的数据格式已经定了，你的接口已经定了，那其实对于你的前后端其实就是隔离的，就是分开的。

那关于对于后端服务的话，其实他的目的就是提供5个接口，第一个接口是创建智能体并调用，直接返回结果或中断数据。那第二个接口是恢复被中断的智能体执行，等待执行完成或再次中断。第三个就是获取当前用户状态，第四个获取系统状态信息，第五个删除用户会话。提供了5个接口，那关于前端业务呢，就是根据你所要实现的一个前端页面的逻辑来去调用这些接口。那我这边画了一个前端的业务流程图，也非常地详细。大家可以根据这个流程图，然后对应地去看相关的源码。那这边的话就给大家稍微去看一下，读一下。首先用户开始之后，那开始也就是你的程序的开始，其实对应的就是你的一个main脚本的开始，也就是在这个地方进去之后，那进去之后呢，首先会调用服务端API接口来获取系统信息。那这个系统信息呢，其实对应的获取对应的接口就是，就是这边的一个获取当前用户的一个状态，这样一个接口，那这个接口所返回的内容就是当前系统活跃会话总数和活跃的用户，所以这个大家可以看一下。在我们的这个运行的这个过程中，我们可以去看一下，会有系统会话这边是0，我们看一个有的，比如说这边系统会话是两个活跃用户，那我们是02和01，所以这边的话，就首先会打印出来这些信息。那紧接着就是等待用户的一个输入ID，那获取到这个用户的输入ID之后，首先他会去叫一个方法，那这个方法呢主要是用来检查并且去尝试自动恢复现有的会话，它会返回只会是有两个值，第一个就是有没有活跃会话，所以它这个是一个布尔值，再一个就是如果有的，他的会话当前的一个状态数据是什么。那他返回这两个之后，那下面就会进入一个循环。那在这个循环里面，主要是根据这两个状态来做相应的一个逻辑的判断。首先，他会判断你当前的是有没有活跃的会话，如果说没有，那这个时候他会往下，如果说有的话，他会走这条之路。那下面我们先来看没有，也就是当前这个用户他没有活跃的会话ID，也就是在你的这个Redis这个会话状态的这个，这个表里面没有相关的数据。那这个时候他就会到下面，到这里，那到这里之后，他做什么样的处理呢，获取用户输入，那输入的话有特殊的指令，也有你可以直接去输入一段文本，那直接去做query。那你输入了相关这个内容之后呢，他就会直接去调用接口，去请求运行智能体，那就去会去开启一个运行这个工作流。那工作流会得到一个response，那并且通过这个方法去统一对智能体回复的内容进行处理。最后呢去调用接口查询当前最新的一个会话状态，也就是在这个函数里面处理完成之后，在处理的过程中呢，他会去更改整个会话的一个状态。那拿到最新的一个状态再去判断当前这完成的一个状态的一个值，是已完成还是说没有，不是已完成，还是在中断状态。所以我们再给大家演示第一个给大家，在第一次给大家演示这个response，也就是大家看接口，也就是在给大家去测试这个的时候，测试这个的时候大家还记得吧，就是我们在第一次产生了一个中断，并且我选择response，然后我把这个问题，这个反馈信息加上去之后，他又触发了一个中断，那这个时候我再去选择yes，然后完成了这次的一个会话。所以这个过程对应的就是这样的一个过程。

好，那这个重置完成之后呢，紧接着还会沿着这条路再去往下，然后流程都是相同的一个流程。那这个里面话又设计了两个方法，那这个方法就是对中断的一个处理，那这个方法呢是对于大智能体回复的统一的处理，我们可以先看一下这个，那关于统一处理智能体响应这边的话，获取到Agent返回response中的一个sessionID和状态这个里面的对象，那这个里面的对象呢，也就是在这个agent response里面，会有一些会话ID，包括当前的状态是中断还是已完成还是错误，包括这个完成时的一个结果的信息，或者中断时的一个中断的一个数据都这个地方，包括你错误时所产生的一个数据。那拿到这个数据之后，你就可以去做相应的判断，比如说你还是去判断它的这个状态，那也是有这几种状态，那每一种状态它的返回值是不一样的，比如说已完成状态，那它就把这个数据对象里面的直接的这个结果的字段直接它返回就可以了。那如果是一个中断状态，它就获取response中的中断数据，进入中断处理函数，也就是又进入到这个中断的这个函数，然后返回一个none回去。那关于一个错误啊，正在运行中啊，或者你的空闲状态，一个位置状态，也会去做相应的一个返回。那紧接着这个中断处理函数的一个逻辑是什么呢，也就在这个地方，也对应它，在这边有一个中断处理逻辑，那中断处理的逻辑的话，主要的一个逻辑就是他会获取到这个用户的输入，也就是你产生了一个中断，中断进入之后，首先它会要求用户输入，也就是要用户反馈嘛。那反馈之后会根据用户输入的内容去做相应的一个逻辑的处理，比如说你是yes，那就是调用接口恢复会话，表示你同意了这个接口，同意了这一次的工具调用。那这个时候大家可以看一下，里面有一个很关键的一个，一个中断类型叫accept这个类型。那如果是其他几个就是它对应的类型是不一样的，大家可以看一下，它这个对应的类型是不一样的。那这个类型主要是用在哪里呢，也就是在用户输入完成之后，它会把这一次的内容返回给你的服务端，那服务端拿到之后，它会去做什么处理呢？就是服务端拿到之后，他会去根据你的返回的这个中断的类型去做不同的处理。比如说这个编辑，那这个编辑的话，我们一旦中断返回的数据，也就是刚刚前端他所封装的这个数据是这样一个数据，那这个时候我们会拿到新的工具的输入，并且通过去调用工具的这个invoke这个方法，来去重新执行工具，并且把工具的最终返回的内容，作为一个结果返回给大模型。所以它对应了四种类型，而这块的逻辑是跟你的前端的这个逻辑是要对起来的。那当然你也可以在这个tools这个脚本里面，去添加你自己的工具或者第三方的MCP server的工具，所以方便你去进行管理。好，那这个呢，就是以上呢就是关于整个的实操的演示和所有的前后端源码的一个分析。那以上的内容呢就是本期视频想给大家去分享的，也希望本期视频的分享对你有所帮助。