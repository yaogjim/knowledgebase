source "https://rubygems.org"

# 使用和 GitHub Pages 兼容的 Jekyll 版本
gem "github-pages", group: :jekyll_plugins

# 可选：如果要使用 Just the Docs 主题，取消下面的注释
# gem "just-the-docs", "~> 0.8.0"

# Jekyll plugins (GitHub Pages 已包含大部分)
group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-sitemap"
end